# Streamlit (frontend)
.streamlit/secrets.toml
*.db

# Python
__pycache__/
*.pyc
.venv/
venv/

# Ambiente
.env
.env.local

# Nunca versionar variaveis de ambiente exportadas do Xano (segredos)
xano-workspace/env/
xano-workspace/*.env.xs

# Segredos que nao devem ser versionados
key.txt
*.key.txt

# Assets de design e e-mail (nao versionados, mantidos so localmente)
email/
design/

# Skills/ferramentas de IA pessoais, nao pertencem ao projeto
.agents/
skills-lock.json

# Estado local de ferramenta, nao deve ser versionado
.claude/scheduled_tasks.lock

# Docs internos (auditoria, testes, modelos de e-mail) - mantidos so localmente
docs/auditoria.md
docs/testes-integracao.md
docs/testes-seguranca.md
docs/emails-templates.md

# Editor / SO
.DS_Store
Thumbs.db
desktop.ini
