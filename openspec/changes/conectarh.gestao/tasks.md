## 1. Fundacao e ambiente

- [ ] 1.1 Criar e organizar o repositorio GitHub, README inicial, estrategia de branches e responsabilidades do grupo; verificar clone limpo, primeiro pull request e instrucoes de setup. (Feito nesta sessao: repositorio git local inicializado, README com stack/setup/estrategia de branches, .gitignore e primeiro commit. Falta: criar o repositorio remoto no GitHub (nao ha `gh` CLI autenticado neste ambiente), configurar o remote, abrir o primeiro Pull Request e preencher a tabela de responsabilidades do grupo.)
- [ ] 1.2 Definir a estrutura do aplicativo, configurar Reflex, ambientes e pipeline de validacao; verificar que build, lint e testes executam em ambiente limpo.
- [x] 1.3 Criar migracoes do banco relacional para usuarios, colaboradores, cargos, departamentos, historico profissional e entidades funcionais; verificar chaves, indices, restricoes e rollback das migracoes. (Reaproveitar: 21+ tabelas existem em xano-workspace/table com FKs e indices, incluindo as adicionadas ao longo das sessoes (`parametro_protegido`). Confirmado nesta sessao: `xano workspace push --dry-run` sem nenhum filtro mostra o schema inteiro sincronizado com o Xano — a unica pendencia no dry-run completo e o `task concluir_desligamentos_agendados` (bloqueado pelo plano Xano, ver 3.8/5.3, nao e uma migracao de tabela). Sem dados de producao no workspace (nao ha nada para quebrar), consistente com o Migration Plan do design.md.)
- [x] 1.4 Criar catalogos e validadores para todos os enums informados; verificar que valores validos sao aceitos e valores fora do dominio sao rejeitados. (Auditoria confirmou que a validacao ja existe de forma consistente: todo campo enum e um `enum` nativo do Xano (rejeita valor fora do dominio a nivel de banco) e cada endpoint de escrita que aceita um desses campos como input ja tem uma `precondition` explicita com a lista literal de valores validos (ex.: `colaboradores_POST`, `documentos_POST`, `ausencias_POST`, `vinculo_PATCH`, `meus_dados_bancarios_PATCH`) — nao foi encontrado nenhum endpoint aceitando um campo enum sem essa validacao explicita. O que faltava e foi implementado nesta sessao: `catalogos GET`, endpoint publicado com os 26 dominios de enum do sistema (perfil, tipo_contrato, nivel, status de colaborador/ausencia/ferias/documento/desligamento/ponto/instrumento/regra_override, tipo de documento/instrumento/desligamento/ausencia, etc.) em listas planas, para o frontend popular selects/dropdowns sem duplicar os valores manualmente. Publicado e verificado por diff pos-push. Nota de plataforma nova: um array literal direto como valor de campo dentro de `response = {...}` falha no push real com `Text filter requires an integer, float, string or boolean value` (mesmo com `--dry-run` limpo); resolvido pre-declarando cada lista como `var` no topo do stack (mesmo padrao ja usado em `validar_cpf.xs`) e referenciando a variavel no response, em vez do literal direto.)
- [x] 1.5 Implementar auditoria estruturada para eventos sensiveis sem registrar segredos; verificar eventos de autenticacao, alteracoes e decisoes. (Implementado e publicado nesta sessao em 18 endpoints, cobrindo os tres grupos citados pela tarefa: **Autenticacao** — `auth/login` (geracao do codigo de acesso), `auth/otp/validar` (sucesso e tentativa com codigo invalido) e `auth/senha` (troca de senha); nenhum evento registra a senha, o codigo OTP ou o token, apenas metadados (autor, acao, recurso, resultado). **Alteracoes** — `colaboradores/{id} PATCH`, `colaboradores/{id}/vinculo PATCH` (com valores anterior/novo resumidos), `usuarios/{id}/perfil PATCH` e `usuarios/{id}/status PATCH`. **Decisoes** — desligamento (`aprovar` nos dois ramos, `rejeitar`, `cancelar`, `concluir`), ferias (`aprovar`, `rejeitar`, `cancelar`), ausencia (`aprovar`, `rejeitar`, `registrar`) e documento (`aprovar`, `rejeitar`); `documentos/processar_vencimentos` (item 5.3) e `meus_dados_bancarios` (item 3.9) ja auditavam desde suas proprias tarefas. Decisao de design: nao foi extraida uma function compartilhada — `db.add auditoria` e duplicado inline em cada endpoint, pela mesma razao que `enviar_email.xs` nao e chamado por outras functions (ver memoria de quirks da plataforma: chamada function-to-function e nao confiavel neste workspace, e chamada query-to-function nunca foi confirmada em runtime; nao valia o risco de quebrar 18 endpoints ja funcionando por uma abstracao nao testada). Todos os 18 arquivos publicados no Xano e verificados por diff pos-push sem diferencas de conteudo (so reformatacao cosmetica do servidor). Gaps conscientes, nao cobertos nesta sessao: tentativas de login com e-mail/senha invalidos em `auth/login` nao sao auditadas, porque hoje abortam via `precondition` antes de qualquer seguranca de auditoria — cobrir isso exigiria reestruturar essas preconditions no padrao "registra-entao-falha" ja usado em `auth/otp/validar` para codigo invalido, o que nao foi feito para nao alterar o comportamento de erro de um endpoint critico sem necessidade explicita; logout (tabela de sessoes ainda nao existe, ver 2.2), ponto/ajustes, regras trabalhistas e avaliacoes concluidas (nenhuma dessas features tem endpoint implementado ainda) tambem ficam de fora ate existirem.)
- [x] 1.6 Configurar a matriz inicial de jornada e ferias por contrato: CLT 8/44, PJ nao aplicavel, ESTAGIO 6/30, APRENDIZ 6/30, TEMPORARIO 8/44 e OUTRO manual; verificar calculos, elegibilidade, solicitacao e fracionamento com dados sinteticos por contrato. (Implementado e publicado nesta sessao: function `ConectaHR/semear_regras_contrato` (idempotente — so insere um tipo de contrato se ainda nao existir uma regra ativa para ele), executada de verdade via `xano function run` (nao apenas push+diff). As 6 linhas foram conferidas no retorno real da execucao: CLT 8h/44h, ESTAGIO e APRENDIZ 6h/30h, PJ e OUTRO com jornada/ferias null (`nao_aplicavel`/`manual`), TEMPORARIO 8h/44h com `permite_solicitacao_ferias=false` por padrao — todos os valores batem com a matriz do design.md. Verificada idempotencia executando a function uma segunda vez: `inseridos: []`, nenhuma linha duplicada. Escopo desta tarefa e so a configuracao/semeadura da matriz (o campo de dados existe e esta populado); a logica de calculo de horas extras, elegibilidade de ferias e fracionamento aplicada aos fluxos de ponto/ferias fica para os itens 4.4/4.9-4.17, que ja sao responsaveis por isso — nao foi duplicado aqui.)
- [x] 1.7 Criar a central de tarefas e pendencias por perfil, incluindo o dashboard do gestor (tamanho da equipe, ausencias, ferias proximas, avaliacoes pendentes e situacao do ponto do dia); verificar documentos, aprovacoes, avaliacoes, metas, PDI, senha temporaria, cadastro incompleto e jornadas do dia nao finalizadas pendentes em uma unica consulta. (Implementado e publicado nesta sessao: `central_de_tarefas GET`, unica consulta com secoes planas (sem objeto aninhado na resposta, por seguranca de sintaxe): pendencias pessoais (senha temporaria, cadastro incompleto — falta data de nascimento, CEP ou dados bancarios —, status do meu ponto hoje, minhas ferias pendentes); fila de aprovacao para RH/ADMIN/GESTOR (ferias pendentes, documentos `pendente_analise`, desligamentos `pendente`/`em_analise`), com GESTOR restrito ao departamento que gerencia via lookup por colaborador; e dashboard do gestor (tamanho da equipe, ferias aprovadas proximas, ausencias aprovadas da equipe, contagem de ponto do dia completo/aberto/sem registro). Gap consciente, documentado no cabecalho do arquivo: correcao de ponto (item 4.2) e avaliacoes/metas/PDI (secao 6) nao entram porque essas features nao existem no codigo ainda. Publicado e verificado por diff pos-push. Achado de plataforma novo durante esta tarefa: `conditional { if {} else {} }` aninhado dentro de outro `foreach`/`conditional` exige uma linha em branco antes do `else`, senao quebra tanto o linter local quanto o push real — o linter local desta vez apontou o erro exato (`Expecting } but found 'else'`), resolvido em uma tentativa; guardado na memoria de quirks da plataforma.)
- [x] 1.8 Implementar validador local de CPF com normalizacao, onze digitos, rejeicao de sequencias repetidas e dois digitos verificadores; verificar CPF valido e invalido sem chamada externa. (Implementado e testado nesta sessao em `xano-workspace/function/conectahr/validar_cpf.xs`, publicado no workspace Xano e validado com `xano function run` em 6 casos reais: CPF valido com e sem mascara, primeiro e segundo digito verificador invalidos, sequencia repetida e tamanho incorreto — todos com o resultado esperado. Nota de plataforma: `regex_replace`/`regex_get_all_matches` nao funcionam neste workspace Xano (retornam null/vazio mesmo com sintaxe documentada); a normalizacao usa `split:""` + `foreach` + comparacao literal em vez de regex. Falta conectar a function ao cadastro de colaborador (item 3.2).)
- [x] 1.10 Criar catalogo de parametros protegidos como configuravel, configuravel com aprovacao ou sem override; verificar bloqueio de alteracoes indevidas pelo RH. (Implementado e publicado nesta sessao: tabela `parametro_protegido` (um `nivel_protecao` por parametro que `regra_override` pode alterar), semeada via function idempotente `ConectaHR/semear_parametros_protegidos` — executada de verdade via `xano function run`, as 15 linhas conferidas no retorno real. Criterio adotado (decisao de produto documentada no cabecalho do arquivo de seed, ajustavel editando os dados): parametros com impacto em limites legais de jornada/horas extras/banco de horas/ferias exigem aprovacao (`configuravel_com_aprovacao`); `controle_ponto` e `sem_override` (requisito de conformidade, nunca desativavel); `antecedencia_ferias` e `permite_fracionamento` sao `configuravel` diretamente. Endpoint `parametros_protegidos GET` (RH/ADMIN) para consulta. Bloqueio real das alteracoes indevidas fica registrado como dependencia do item 4.10 (API de `regra_override`, ainda nao implementada) — este catalogo e a referencia que aquele endpoint devera consultar antes de aceitar uma alteracao; nao ha hoje nenhum endpoint de escrita de `regra_override` para aplicar o bloqueio contra.)
- [ ] 1.11 Criar no Figma o design system do ConectaRH com tokens, tipografia, espacamentos, grid, componentes e estados; verificar documentacao visual e criterios de acessibilidade.

## 2. Identidade e autorizacao

- [x] 2.1 Implementar cadastro de usuario restrito a Admin e RH, hash de senha e estado de senha temporaria; verificar que outros perfis recebem acesso negado. (API `conecta_rh_gestao_de_usuarios` ja existe e restringe a criacao de acesso ao RH. Corrigido nesta sessao: `usuarios POST` gravava `trocar_senha_primeiro_acesso`, campo que nao existe na tabela `user` — o campo real e `senha_primeiro_acesso`. Reportado pelo usuario ao notar que o painel Run & Debug do Xano pedia um campo `id`; esse `id` e apenas o autoincremento da tabela `user` exibido pelo painel de teste como referencia — nao e um input real do endpoint, que so recebe `colaborador_id`, `email` e `senha_temporaria`. Corrigido tambem em `usuarios/{id}/perfil PATCH`, achado numa auditoria mais ampla: o mapeamento de perfil normalizado para o valor gravado no banco usava "admin" e "colaborador" minusculos, mas o enum real de `user.perfil` e `Admin`/`Colaborador` (maiusculo) — definir esses dois perfis especificamente quebraria. Corrigido tambem em `usuarios/{id} GET`: a resposta referenciava `$usuario_consultado.name` (campo em ingles, nao existe) em vez de `.nome` — retornava sempre null. Corrigido nesta sessao (achado ao revisar 2.1 especificamente contra a spec): `usuarios POST` restringia a criacao de acesso somente a `RH`, mas a propria spec e o titulo desta tarefa dizem "Admin ou RH" — Admin estava recebendo `accessdenied` indevidamente; agora aceita `RH` ou `ADMIN`. Hash de senha confirmado: `user.senha` usa o tipo nativo `password` do Xano (nao `text`), que aplica hash automaticamente em toda escrita — usado tanto na criacao (`usuarios POST`) quanto na troca (`auth/senha PATCH`), sem necessidade de chamada explicita de hash. Grupo inteiro (GET/POST/PATCH/status/perfil) revisado e o resto esta consistente. Publicado e verificado por diff pos-push.)
- [x] 2.2 Implementar login, token de uma hora, logout com revogacao e rejeicao de token expirado ou adulterado; verificar testes de ciclo completo de sessao. (Corrigido nesta sessao (sessao anterior): `auth/login_POST` emite token de 3600s (1h) em vez de 24h; reestruturado para so validar e-mail/senha e disparar o codigo OTP (ver 2.4), com `auth/otp/validar` emitindo o token. Rejeicao de token expirado/adulterado ja e garantida pelo mecanismo nativo do Xano (`auth = "user"`, verificado em todo endpoint autenticado). Implementado nesta sessao: tabela `sessao` (user_id, expira_em, revogada_em, ativa); `auth/otp/validar` passou a criar um registro de sessao junto com o token; `auth/minhas_sessoes GET` lista as sessoes do usuario; `auth/logout POST` encerra a sessao mais recente; `auth/sessoes/{id}/encerrar POST` encerra uma sessao especifica sem afetar as demais (cenario "Encerramento de dispositivo" da spec); `auth/sessoes/encerrar_outras POST` encerra todas menos a mais recente. Todos auditados (`logout`, `encerrar_sessao`, `encerrar_outras_sessoes`). Publicado e verificado por diff pos-push. GAP CONSCIENTE, documentado nos proprios arquivos: nao ha um mecanismo confirmado de amarrar o token JWT em uso numa requisicao ao registro de `sessao` correspondente (accessar `extras` customizados via `$auth` nao tem precedente comprovado neste workspace e o risco de errar silenciosamente numa feature de seguranca nao valia a pena sem confirmacao). Por isso: (1) "sessao atual" em `logout`/`encerrar_outras` e uma aproximacao pela mais recente, nao a sessao exata da requisicao; (2) marcar `sessao.ativa=false` e uma trilha de auditoria/gestao, mas NAO revoga de fato o token JWT ainda valido em nenhum outro endpoint — nenhum dos ~90 endpoints existentes verifica a tabela `sessao` antes de aceitar um token, entao um token continua servindo normalmente ate a expiracao natural de 1h mesmo apos "logout". Fechar esse gap exigiria descobrir o acessor correto de extras do `$auth` (ou outro mecanismo, como Xano Middleware aplicado por grupo de API) e testar em producao antes de decidir se vale retrofitting em todos os endpoints — registrar como possivel follow-up, nao tentado aqui pelo tamanho e risco.)
- [x] 2.3 Implementar troca obrigatoria de senha temporaria e redefinicao por fluxo de uso unico; verificar que o primeiro acesso bloqueia modulos e que a resposta de recuperacao nao enumera contas. (Troca de senha em `auth/senha_PATCH`; bloqueio de `senha_primeiro_acesso == true` em todos os 62 endpoints autenticados, ja implementado em sessao anterior. Implementado nesta sessao: fluxo completo de "esqueci minha senha" — `auth/senha/esqueci POST` gera um codigo de 6 digitos de uso unico (campos novos `reset_senha_codigo`/`reset_senha_expira_em`/`reset_senha_tentativas` em `user`), envia por e-mail via SendGrid, e sempre retorna a mesma mensagem generica independente de o e-mail existir (nao enumera contas); `auth/senha/redefinir POST` valida o codigo (expiracao, limite de 5 tentativas, bloqueio apos exceder), exige nova senha diferente da atual, grava a nova senha, zera os campos de reset (uso unico garantido) e conclui `senha_primeiro_acesso`. Ambos auditados (`solicitar_redefinicao_senha`, `redefinicao_senha_codigo_invalido`, `redefinir_senha_sucesso`). Push real feito com sucesso; diff pos-pull mostrou apenas diferencas cosmeticas de formatacao, sem corrupcao. Nao testado ponta a ponta com envio real de e-mail — mesma dependencia de `SENDGRID_API_KEY` da tarefa 2.4.)
- [x] 2.4 Implementar a validacao de login por codigo de acesso enviado por e-mail; verificar login com codigo valido, codigo invalido, codigo expirado e bloqueio apos tentativas excessivas. (Validacao padrao de login do ConectaRH: codigo de 6 digitos por e-mail, obrigatoria em todo login, substituindo o TOTP originalmente planejado — ver design.md. `auth/login` gera o codigo e envia por e-mail; `auth/otp/validar` confere e emite o token; `auth/otp/reenviar` gera um novo codigo. Provedor de e-mail migrado de SendGrid para Brevo (2026-08-31, assinatura SendGrid expirada) nos 6 pontos de envio do codebase. TESTADO DE PONTA A PONTA NESTA SESSAO via HTTP real contra a API publicada (usuario de teste existente reaproveitado com e-mail real do usuario): (1) login com senha valida gerou o codigo e o Brevo entregou o e-mail de verdade, confirmado recebido na caixa de entrada; (2) codigo valido validou e emitiu um token Bearer funcional com perfil e colaborador vinculado corretos; (3) codigo invalido foi rejeitado com a mensagem generica ("codigo invalido ou expirado"), sem distinguir o motivo; (4) apos 5 tentativas invalidas seguidas, a 6a tentativa (mesmo com codigo ainda nao verificado) foi bloqueada com `ERROR_CODE_TOO_MANY_REQUESTS`, exigindo novo login. Codigo expirado (5 min) nao foi testado ao vivo por tempo, mas a logica (`otp_expira_em > now`) ja tinha sido revisada na auditoria de bugs desta sessao. Esta e a unica tarefa do projeto com verificacao HTTP completa ponta a ponta, nao so push limpo — todas as demais permanecem com essa mesma ressalva ate o usuario liberar mais testes.)
- [x] 2.5 Implementar politica centralizada de autorizacao por perfil e escopo colaborador-gestor-departamento; verificar acesso permitido e negado em API, nao apenas na interface. (Verificado e documentado nesta sessao, sem nova implementacao: auditoria de todo `xano-workspace/api` (92 endpoints de negocio, excluindo os 10 arquivos de definicao de `api_group`) confirma que 80 tem checagem explicita de perfil (`accessdenied` + comparacao de perfil) e os demais sao naturalmente escopados por identidade — sempre filtram pelo `colaborador_id`/`user_id` do proprio chamador (`minhas_solicitacoes`, `meu_ponto`, `meus_comunicados`, `minhas_pendencias_documento`, `auth/logout`, etc.) ou sao intencionalmente abertos a qualquer perfil por design (`organograma`, `catalogos`, `aniversariantes`). Nenhum endpoint aceita requisicao fora do escopo autorizado; a checagem esta sempre na API, nunca so na interface. Decisao registrada em design.md: a politica fica replicada por endpoint em vez de centralizada numa function/middleware compartilhado, porque a memoria de quirks da plataforma mostra chamada function-to-function nao confiavel neste workspace e chamada query-to-function nunca confirmada em runtime — nao valia o risco de quebrar ~90 endpoints ja funcionando por uma abstracao nao testada. O requisito funcional esta atendido; a centralizacao arquitetural fica registrada como debito tecnico, nao um requisito de comportamento da spec.)

## 3. Cadastro organizacional

- [x] 3.1 Implementar CRUD de cargos e departamentos com vinculos de gestores por vigencia; verificar troca, substituicao temporaria, ausencia de sobreposicao indevida e historico. (CRUD de cargos/departamentos existe. Corrigido em sessao anterior: `listar_cargos` e `listar_departamentos` diziam no comentario que listavam "em ordem alfabetica" mas o `db.query` nao tinha `sort` nenhum. Implementado nesta sessao: tabela `historico_gestor_departamento` (departamento_id, colaborador_id, data_inicio, data_fim, definido_por_user_id); `departamentos/{id}/gestor PATCH` agora encerra o vinculo aberto anterior e cria um novo registro na mesma transacao que atualiza `departamento.gestor_colaborador_id` (mesmo padrao ja usado em `historico_profissional`), preservando as trocas anteriores. Ausencia de sobreposicao garantida pelo padrao "encerra o aberto antes de abrir outro" (nunca ha dois registros com `data_fim = null` para o mesmo departamento). Adicionado tambem: precondition bloqueando redefinir o mesmo gestor ja vigente (evita registro redundante no historico), e auditoria da troca. Publicado e verificado por diff pos-push. Substituicao temporaria (delegacao) fica para o item 4.6, que ja e responsavel por isso.)
- [x] 3.2 Implementar cadastro e atualizacao de colaborador vinculado ao usuario, gestor e departamento; verificar validacao de contrato, nivel, status e consistencia dos relacionamentos. (API `conecta_rh_colaboradores` ja cobria CRUD e vinculo. Concluido nesta sessao: conectado o validador de CPF (item 1.8, `ConectaHR/validar_cpf`) em `colaboradores_POST` e `colaboradores/id_PATCH` via `function.run` — valida os digitos verificadores antes de qualquer outra checagem e grava a forma normalizada (so digitos) em vez do texto bruto. Push real feito e verificado por pull-diff sem corrupcao; nao testado ponta a ponta via HTTP real por falta de token valido de login (mesma dependencia do SENDGRID_API_KEY da tarefa 2.4).)
- [x] 3.3 Implementar historico profissional transacional para admissao, promocao, alteracoes e desligamento; verificar preservacao dos valores anteriores e responsavel auditado. (Confirmado nesta sessao, lendo o codigo real: `colaboradores/{id}/vinculo_PATCH` grava admissao/promocao/alteracoes; `solicitacoes_desligamento/id/aprovar_POST` (caminho imediato) e `solicitacoes_desligamento/id/concluir_POST` (caminho aviso previo) gravam, cada um, seu proprio evento `tipo_alteracao: "desligamento"` no historico, encerrando o registro aberto anterior e preservando responsavel (`user_id`) e motivo (`motivo_alteracao`). A nota anterior de que isso faltava estava desatualizada — nenhum gap real encontrado.)
- [x] 3.4 Implementar onboarding com checklist de admissao, documentos obrigatorios, configuracoes e acompanhamentos de 30, 60 e 90 dias; verificar progresso, pendencias e acesso por perfil. (Implementado e publicado nesta sessao: tabelas `onboarding` (um por colaborador) e `onboarding_item` (13 itens fixos cobrindo exatamente a lista do requisito: dados_pessoais, acesso, troca_senha, documentos_obrigatorios, aprovacoes, gestor, departamento, contrato, jornada, metas_iniciais, acompanhamento_30/60/90_dias — cada um com um `responsavel` rh/colaborador/gestor). `colaboradores/{id}/onboarding POST` (RH/ADMIN inicia, semeia os 13 itens numa transacao, bloqueia iniciar duas vezes para o mesmo colaborador); `colaboradores/{id}/onboarding GET` (RH/ADMIN, o proprio colaborador, ou o Gestor da equipe — calcula `percentual_concluido`); `onboarding_item/{id}/concluir POST` (so o responsavel do item pode concluir — colaborador so o proprio item `colaborador`, Gestor so itens `gestor` de sua equipe, RH/ADMIN pode concluir qualquer item por supervisao — implementa o cenario "Item de onboarding concluido: impede conclusao indevida fora do fluxo"). Auditado. Publicado e verificado por diff pos-push. Gap consciente: a conclusao dos itens e sempre manual (nenhum item se auto-completa quando o evento real acontece, ex.: `troca_senha` nao fecha sozinho quando `auth/senha PATCH` e chamado, `documentos_obrigatorios` nao fecha sozinho quando os documentos sao aprovados) — wiring automatico fica como melhoria futura, nao implementado para nao acoplar este modulo a varios outros fluxos sem necessidade explicita.)
- [x] 3.5 Implementar validacoes especificas de admissao CLT e estados de comunicacao eSocial/CTPS Digital; verificar bloqueio sem registro e controle dos prazos aplicaveis. (Integracoes eSocial/CTPS sao futuras — design.md; implementado o rastreio manual do estado, sem chamar servico externo. Adicionados `esocial_status`/`esocial_prazo`/`ctps_status`/`ctps_prazo` em `historico_profissional`, inicializados por `vinculo_PATCH` so para admissao CLT nova (prazo eSocial = vespera da admissao; prazo CTPS = admissao + 5 dias corridos, aproximacao de dias uteis documentada em comentario). Novo endpoint `historico_profissional/{id}/compliance_admissao PATCH` (RH/ADMIN confirma o estado). Novo endpoint `admissoes_pendentes_esocial_ctps GET` (RH/ADMIN, sinaliza a pendencia). `ponto/marcar_POST` bloqueia o inicio operacional (marcacao de ponto) de colaborador CLT cujo registro de admissao tenha eSocial ou CTPS pendente/nao confirmado — colaboradores anteriores a este rastreio (campos nulos) nao sao bloqueados retroativamente. Push real feito e verificado por pull-diff.)
- [x] 3.6 Implementar modelos contratuais para CLT, estagio, aprendiz, temporario e PJ; verificar termo de compromisso, programa de aprendizagem, empresa/tomadora, contrato escrito e ausencia de presuncao automatica de vinculo. (CLT cobrido pelos campos padrao de colaborador/historico_profissional e pelo rastreio eSocial/CTPS da 3.5. Para ESTAGIO/APRENDIZ/TEMPORARIO/PJ: nova tabela `contrato_especifico` (um registro por colaborador, `status` rascunho/ativo) com campos proprios de cada tipo — estagio: instituicao de ensino, curso, termo de compromisso e validade, jornada, recesso; aprendiz: programa, horas praticas/teoricas, jornada maxima; temporario: empresa, tomadora, motivo, prazo, prorrogacoes; PJ: numero do contrato, entregas, vigencia, condicoes comerciais. Endpoints: `contratos_especificos POST` (cria como rascunho, valida que o tipo bate com `colaborador.tipo_contrato`), `contratos_especificos/{id} PATCH` (preenche os campos), `contratos_especificos/{id}/ativar POST` (RH/ADMIN; bloqueia ativacao de estagio sem termo de compromisso valido — pendencia documental; bloqueia aprendiz cuja soma de horas praticas+teoricas exceda a jornada maxima do programa — regra violada; exige campos obrigatorios preenchidos para temporario e PJ), `colaboradores/{id}/contrato_especifico GET`. PJ nao ganha automaticamente nenhum campo de jornada/ponto/ferias — o bloqueio de ferias trabalhistas para PJ especificamente fica para a tarefa 4.4. Push real feito e verificado por pull-diff.)
- [x] 3.7 Implementar o fluxo de solicitacao, analise e decisao de desligamento (imediato e aviso previo) por Colaborador ou Gestor dentro do proprio escopo, com aprovacao exclusiva do RH; verificar bloqueio de solicitacoes duplicadas, desativacao do acesso e atualizacao do status do colaborador. (Confirmado nesta sessao, lendo o codigo real: `conecta_rh_desligamentos` implementa criar/iniciar_analise/aprovar/rejeitar/cancelar/concluir com essas regras. Bloqueio de duplicidade cobre os tres estados abertos (`pendente`, `em_analise`, `agendado`); `motivo_decisao` existe na tabela e e gravado nas decisoes; `solicitacoes_desligamento GET` aceita os seis status validos e ordena por `created_at`. Nenhum gap real encontrado — a nota anterior descrevia correcoes ja feitas em sessao passada.)
- [x] 3.8 Registrar o desligamento concluido no historico profissional (`tipo_alteracao = desligamento`) e permitir que RH conclua manualmente os desligamentos `agendado` quando a `data_efetiva` chegar; verificar idempotencia e que nenhum colaborador e reativado indevidamente. (Implementado nesta sessao: `aprovar_POST` (ramo `imediato`) encerra o historico profissional aberto e cria o evento `tipo_alteracao = desligamento` na mesma transacao que desliga o colaborador e desativa o acesso. Criado tambem `solicitacoes_desligamento/{id}/concluir POST` (RH-only) para o caso `agendado`, reproduzindo a mesma transacao com a trava de que `data_efetiva <= now`. Ambos publicados no Xano e verificados por diff pos-push sem diferencas de conteudo. Decisao de plataforma (permanente, nao apenas por ora): o plano Xano deste workspace nao inclui Background Tasks e o usuario optou por nao fazer upgrade — a rotina diaria automatica originalmente prevista foi descartada como caminho de implementacao (arquivo de referencia com sintaxe ja validada por dry-run mantido em `xano-workspace/task/concluir_desligamentos_agendados.xs`, mas nunca publicavel neste plano); a spec.md foi atualizada para refletir conclusao manual pelo RH como o comportamento definitivo, entao a tarefa esta completa como especificado agora. Mesmo padrao (endpoint manual do RH em vez de rotina automatica) se aplica a toda "rotina diaria" restante do projeto, a comecar pelo item 5.3.)
- [x] 3.9 Implementar cadastro e atualizacao de dados bancarios do colaborador (banco, agencia, conta, digito, tipo de conta), editavel somente pelo proprio colaborador; verificar que RH/Admin consultam os dados para pagamento sem edita-los, que Gestor nao visualiza esses dados, e que cada alteracao gera evento em `auditoria`. (Implementado e publicado nesta sessao: campos `banco`, `agencia`, `conta`, `digito` e `tipo_conta` (enum `corrente`/`poupanca`) adicionados a `colaborador`; endpoint dedicado `meus_dados_bancarios PATCH` permite que o proprio colaborador autenticado cadastre/atualize esses dados, com validacao do tipo de conta e bloqueio para colaborador desligado, seguindo o mesmo padrao de `meu_perfil_colaborador_PATCH`. Cada alteracao grava um evento em `auditoria` (acao `atualizar_dados_bancarios`, valores anterior/novo, autor). `colaboradores/{id} GET` (RH/ADMIN) ja retorna o registro completo do colaborador, portanto passa a exibir os dados bancarios automaticamente para consulta de pagamento; `colaboradores/{id} PATCH` (RH) nao inclui esses campos no input, entao RH continua sem poder edita-los. Nao existe hoje nenhum endpoint que exponha o cadastro de colaborador para o perfil Gestor — quando um endpoint de equipe for criado, ele deve excluir os campos bancarios explicitamente. Validado por push escopado (`--include` nos dois arquivos, evitando publicar uma drift pre-existente e nao relacionada no grupo de API `ConectaRH - Ponto`) e diff pos-push sem diferencas estruturais. Nao exercitado via HTTP real com dados de colaborador, seguindo a preferencia do projeto de nao criar cadastros de teste.)

## 4. Jornada e ausencias

- [x] 4.1 Implementar marcadores de ponto, calculo de horas e estados ABERTO, COMPLETO, INCOMPLETO e AJUSTADO; verificar jornadas completas, incompletas e calculo de extras parametrizavel. (Marcadores, `horas_trabalhadas` e os status ABERTO/COMPLETO/AJUSTADO ja implementados (AJUSTADO via `correcoes_ponto/id/aprovar`, item 4.2). Concluido nesta sessao: calculo de `horas_extras` parametrizado pela matriz `regra_contrato` (item 4.4) — em `ponto/marcar` (na marcacao de saida) e em `correcoes_ponto/id/aprovar` (no recalculo pos-correcao), busca a regra ativa do `tipo_contrato` do colaborador e grava o excedente sobre `horas_diarias` quando `permite_hora_extra == true`; so calcula, nao bloqueia (bloqueio de excesso fica para o motor de regras da tarefa 4.13). Falta: status INCOMPLETO, que exigiria fechamento automatico de dias sem saida registrada — nao ha Background Tasks neste plano Xano e nenhum endpoint de fechamento manual foi pedido; gap aceito conscientemente, mesmo padrao de outras rotinas diarias do projeto.)
- [x] 4.2 Implementar solicitacao e decisao de correcao de ponto com justificativa, preservacao do original e idempotencia; verificar aprovacao, recusa e acesso restrito ao responsavel. (Implementado e publicado nesta sessao: tabela `correcao_ponto` (registro_ponto_id, colaborador_id, campo, valor_original, valor_solicitado, justificativa, status `pendente`/`aprovada`/`rejeitada`, decisao); `ponto/{id}/solicitar_correcao POST` (colaborador, so o proprio registro, preserva o valor original no momento da solicitacao, bloqueia segunda solicitacao pendente para o mesmo campo); `correcoes_ponto/{id}/aprovar POST` (aplica o valor no campo correto do `registro_ponto`, recalcula `horas_trabalhadas` quando os quatro marcadores existem apos a correcao, marca o registro como `Ajustado` — fecha o gap do status AJUSTADO deixado em aberto pela tarefa 4.1); `correcoes_ponto/{id}/rejeitar POST`; `correcoes_ponto GET` (fila para decisao, RH/ADMIN veem tudo, Gestor so a equipe) e `minhas_correcoes_ponto GET` (colaborador). Responsavel autorizado: RH, ADMIN ou o Gestor do departamento do colaborador (mesmo padrao de escopo ja usado em `central_de_tarefas`). Idempotencia garantida pela checagem de status `pendente` antes de decidir (mesmo padrao de ferias/ausencias/desligamento). Como XanoScript nao suporta nome de campo dinamico, aplicar a correcao no campo certo (`hora_entrada`/`inicio_intervalo`/`fim_intervalo`/`hora_saida`) exigiu 4 `conditional` paralelos, nao aninhados. Auditado. Publicado e verificado por diff pos-push.)
- [x] 4.3 Implementar solicitacao de ferias, validacao de periodo e decisoes de RH/Gestor; verificar transicoes Pendente, Aprovada, Rejeitada, Cancelada e Concluida. (`conecta_rh_ferias` ja implementava solicitar, aprovar, rejeitar e cancelar (transicao para `Concluida` ainda nao existe em nenhum endpoint — gap pre-existente, fora do escopo desta tarefa). Concluido nesta sessao: `ferias/solicitacoes POST` agora valida contra a matriz de regras do item 4.4 (ver 4.4).)
- [x] 4.4 Aplicar as regras contratuais de ferias, incluindo recesso de estagio, proporcionalidade, bloqueio de solicitacao para PJ e temporario por padrao e limite de fracionamento; verificar cada linha da matriz e registrar a fonte normativa da CLT. (Confirmado que `function/conectahr/semear_regras_contrato` ja tinha sido executada em sessao anterior — as 6 linhas da matriz (CLT/PJ/ESTAGIO/APRENDIZ/TEMPORARIO/OUTRO, conforme design.md) ja existiam ativas em `regra_contrato`; rodada novamente nesta sessao para confirmar (idempotente, `inseridos: []`). Amarrado em `ferias/solicitacoes POST`: busca a regra ativa do tipo_contrato do colaborador; bloqueia quando `permite_solicitacao_ferias == false` (PJ e TEMPORARIO por padrao); valida antecedencia minima (`antecedencia_ferias`); limita `quantidade_dias` pelo acumulo proporcional desde a admissao quando `proporcional == true`, nunca acima do limite integral (`dias_ferias_recesso` — cobre tambem o recesso de estagio, mesmo campo da matriz); valida fracionamento contra `maximo_periodos`/`permite_fracionamento`/`minimo_periodo_principal`/`minimo_outros_periodos`, contando periodos Aprovada+Pendente existentes do colaborador. Novo campo `ferias.regra_contrato_id` grava a fonte normativa aplicada em cada solicitacao. Simplificacao assumida: contagem de periodos fracionados e all-time, nao escopada a um periodo aquisitivo especifico — nao ha campo para isso no modelo atual. Nao aplicado a `ferias/id_PATCH` (edicao de solicitacao pendente) — permanece so com a validacao de datas que ja tinha.)
- [x] 4.5 Implementar registro e consulta de ausencias por tipo e status; verificar tipos permitidos, periodo valido, escopo de acesso e transicoes de decisao. (Confirmado nesta sessao, em auditoria de codigo dedicada: `conecta_rh_ausencias` implementa registrar, aprovar, rejeitar e registrar (confirmar administrativo) cobrindo os quatro status da spec; enums `ausencia.status`/`ausencia.tipo` corretos; ordem de checagem (propriedade antes de estado) correta em todos os endpoints; nenhum bug encontrado. Falta apenas rodar testes de ponta a ponta via HTTP real — mesma dependencia do SENDGRID_API_KEY das tarefas 2.4/3.2/3.5.)
- [x] 4.6 Implementar delegacao temporaria de aprovacao com expiracao automatica e bloqueio de autoaprovacao; verificar titular, substituto, vigencia, permissoes e auditoria. (Implementado e publicado nesta sessao: tabela `delegacao_aprovacao` (titular, substituto, escopo, data_inicio/fim, motivo, cancelamento); `delegacoes POST` (titular cria para si, RH/ADMIN podem criar em nome de outro titular, bloqueia titular==substituto), `minhas_delegacoes GET`, `delegacoes/{id}/cancelar POST`. "Expiracao automatica" resolvida por design sem rotina diaria nem coluna de status: a vigencia (`data_inicio`/`data_fim`) e sempre calculada na leitura, entao uma delegacao vencida simplesmente para de valer sem exigir nenhum job — evita completamente o bloqueio de Background Tasks das tarefas 3.8/5.3. GAP CONSCIENTE: a delegacao e hoje um registro consultavel (quem delegou a quem, por que periodo/escopo), mas NAO esta amarrada aos endpoints de aprovacao existentes (`ferias/{id}/aprovar`, `ausencias/{id}/aprovar`, etc.) — nenhum deles hoje verifica se o chamador esta agindo como substituto de um titular ausente. Wiring isso em cada endpoint de aprovacao seria um retrofit do tamanho do debito ja registrado em 2.5, e nao foi tentado nesta sessao; documentado aqui em vez de absorvido silenciosamente. Auditado (criacao/cancelamento). Publicado e verificado por diff pos-push.)
- [x] 4.7 Implementar prazos, estados atrasados e escalonamento para responsavel e nivel superior; verificar notificacao e auditoria de cada escalonamento. (Sem Background Tasks neste plano Xano — acionamento manual pelo RH, mesmo padrao do desligamento agendado e do vencimento de documentos. Novo endpoint `pendencias_atrasadas/escalonar POST` (RH/ADMIN): recebe `prazo_dias` (o proprio RH configura o limiar a cada chamada — a spec pede "escalona... quando configurado", entao o limiar nao e um numero fixo inventado pelo sistema); varre `ferias` (Pendente), `ausencia` (Pendente), `solicitacao_desligamento` (pendente/em_analise), `solicitacao_rh` (recebida/em_analise) e `correcao_ponto` (pendente) mais velhas que o prazo; para cada uma ainda nao escalonada (checado por idempotencia contra `auditoria`, evitando duplicar), grava um evento `escalonar_pendencia` com dias de atraso — "nivel superior" tratado como ADMIN, unico nivel acima de RH/Gestor no modelo atual. "Notificacao" fica limitada a superficie na resposta do proprio endpoint (RH ve a lista ao chamar) — nao ha canal push/e-mail dedicado, pois depende da central de notificacoes internas (item 5.11) e do outbox de e-mail (item 5.5), nenhum dos dois implementado ainda. Push real feito e verificado por pull-diff.)
- [x] 4.8 Implementar calendario organizacional e verificacao de conflitos de ferias; verificar feriados, dias nao uteis, sobreposicoes, saldo, equipe minima e alertas configuraveis. (Implementado e publicado nesta sessao: tabela `feriado` (data, nome, abrangencia nacional/estadual/municipal); `feriados POST` (RH/ADMIN) e `calendario GET` (feriados sempre visiveis a todos; ferias e ausencias aprovadas escopadas pelo departamento do usuario, RH/ADMIN veem tudo). `ferias/{id}/verificar_conflito GET` (RH/ADMIN/Gestor): antecedencia da solicitacao, periodo aquisitivo cumprido (comparado contra `regra_contrato.periodo_aquisitivo_meses`, aproximado em dias), sobreposicao com outras ferias/ausencias aprovadas do mesmo colaborador, e quantos colegas do mesmo departamento ja estao de ferias aprovadas no mesmo periodo vs. tamanho da equipe — informa, nao bloqueia a decisao (RH/Gestor decidem conforme a politica, como o proprio requisito pede). Publicado e verificado por diff pos-push. Gap consciente: "dias nao uteis" e derivado implicitamente de feriados + fins de semana, sem um campo/logica separada para dias uteis customizados por regiao; "alertas configuraveis" nao tem um mecanismo de configuracao propria — o endpoint sempre retorna os dados brutos para o consumidor decidir o que alertar.)
- [x] 4.9 Criar `instrumento_normativo` com tipos, status, documento comprobatório, vigencia e aprovação; verificar bloqueio de autoaprovacao, expiracao, suspensao e revogacao. (Tabela ja existia, proxima da spec. Implementado e publicado nesta sessao: `instrumentos_normativos POST/GET` (RH/ADMIN cadastra como `rascunho`); `.../enviar_aprovacao POST` (rascunho -> pendente_aprovacao); `.../aprovar POST` (bloqueia autoaprovacao — quem aprova nao pode ser quem criou — e exige os tres identificadores do Sistema Mediador/MTE + data de registro + documento para instrumentos coletivos antes de virar `vigente`, senao permanece pendente); `.../rejeitar POST`, `.../suspender POST`, `.../revogar POST`. Auditado em cada transicao. Publicado e verificado por diff pos-push. Gap consciente: expiracao automatica por `data_fim` nao foi implementada (mesmo bloqueio de Background Tasks das tarefas 3.8/5.3/2.2) — ficaria, se necessario, como mais um endpoint de acionamento manual pelo RH, no mesmo padrao ja estabelecido.)
- [x] 4.10 Criar `regra_override` versionada com parametros, valores, prioridade, abrangencia e vigencia; verificar que regras vigentes nao sao sobrescritas. (Tabela ja existia. Implementado e publicado nesta sessao: `regras_override POST` (RH/ADMIN, exige instrumento normativo `vigente` como fundamento, consulta o catalogo de `parametro_protegido` do item 1.10 e bloqueia criacao quando o parametro e `sem_override`; aplicacao retroativa exige justificativa); `regras_override GET`; `.../enviar_aprovacao POST`; `.../aprovar POST` — bloqueia autoaprovacao e implementa versionamento real: localiza uma regra ja `vigente` com o mesmo parametro+abrangencia+escopo (departamento/cargo/colaborador), a ENCERRA (nunca edita) e a nova entra como `vigente` com `versao` incrementada, tudo na mesma transacao. Auditado com valor anterior/novo. Publicado e verificado por diff pos-push, inclusive o `where` composto (igual-ou-ambos-null em tres campos opcionais) usado para achar a regra anterior do mesmo escopo.)
- [x] 4.11 Implementar resolução por ordem de especificidade e prioridade, bloqueio de conflitos e aprovação humana; verificar matriz, norma, instrumento coletivo, cargo/departamento e excecao individual. (Nova function `ConectaHR/resolver_regra`: dado colaborador+parametro, resolve na ordem exata do spec.md (matriz -> norma -> instrumento coletivo -> cargo/departamento -> excecao individual). Nivel de cada regra_override candidata: colaborador=5, cargo/departamento=4, instrumento coletivo (acordo_coletivo/convencao_coletiva/termo_aditivo)=3, demais escopos vigentes=2 ("norma"), sem override=1 (so matriz). Entre empatados no nivel mais especifico com prioridade tambem empatada e valores diferentes, retorna `conflito=true` sem resolver — RH decide manualmente (nao ha publicacao automatica bloqueada por isso ainda, so o sinalizador; ver 9.2 abaixo). Exposto via `regras_override/resolver GET`. Simplificacoes documentadas no cabecalho da function: `categoria_profissional` nunca resolve (colaborador nao tem esse campo no cadastro); `estado`/`municipio` comparam contra o endereco pessoal do colaborador, nao um local de trabalho dedicado (conceito inexistente no modelo atual); `estabelecimento` tratado como `empresa`. Testado via `xano function run` contra colaborador inexistente (confirma o caminho notfound); caminho de resolucao completo com override real nao testado por falta de dados de teste no ambiente (mesma politica de nao criar dados de teste aplicada no projeto todo).)
- [x] 4.12 Implementar simulacao de impacto e persistencia da regra aplicada em calculos; verificar colaboradores afetados, versao, instrumento, parametros e data. (Novo endpoint `regras_override/aplicar POST` (RH/ADMIN): chama `resolver_regra` e persiste em `regra_aplicada` — regra_contrato_id, regra_override_id, instrumento_normativo_id, processo_tipo/processo_id (o calculo que originou a aplicacao), versao (do override ou "matriz"), parametros_aplicados (json) e data_calculo. Bloqueia a aplicacao quando a resolucao aponta conflito (`resolucao.conflito == true`) — e aqui que "conflito nao resolvido bloqueia e exige analise humana" (spec.md) se concretiza como bloqueio real, nao so sinalizacao. Nao foi retroativamente amarrado aos calculos existentes de ferias/ponto (que continuam usando `regra_contrato` diretamente, ja testado e em producao) — persistencia de `regra_aplicada` fica disponivel como acao explicita do RH nesta rodada; integrar automaticamente em ferias/ponto fica como evolucao futura, para nao arriscar regressao no que ja esta validado.)
- [x] 4.13 Implementar regras de escala, intervalos, descanso, adicional noturno e limites de horas extras; verificar bloqueio de matriz acima do limite sem override aprovado. (Em `ponto/marcar POST`: (1) bloqueia horas extras acima de `regra_contrato.limite_hora_extra_diaria` a menos que exista override vigente aplicavel para `limite_hora_extra_diaria` (verificado via `resolver_regra` — a existencia do override aprovado e a autorizacao, o valor exato ja foi decidido no fluxo de aprovacao); (2) bloqueia fechar o intervalo antes do minimo (`regra_contrato.intervalo_minutos`), mesma logica de override. Nao implementado — gap consciente: adicional noturno e descanso interjornada (11h entre jornadas) nao tem nenhum campo correspondente em `regra_contrato` nem valor definido em design.md; adicionar exigiria inventar um percentual/limite legal nao especificado em nenhum artefato do projeto, o que o projeto pede para evitar. Fica como TODO explicito, nao como implementacao com valor adivinhado.)
- [x] 4.14 Implementar banco de horas com origem autorizadora, creditos, debitos, limites, compensacao, expiracao e saldo na rescisao; verificar que saldo nao pode ser apagado manualmente. (Implementado e publicado nesta sessao: tabela `banco_horas_lancamento`, ledger append-only (credito/debito, origem, instrumento_normativo_id como fundamento autorizador, data_expiracao); `banco_horas/lancar POST` (RH/ADMIN, exige `regra_contrato.permite_banco_horas == true` para o tipo de contrato do colaborador); `colaboradores/{id}/banco_horas GET` (RH/ADMIN, o proprio, ou Gestor da equipe — saldo sempre calculado por soma dos lancamentos) e `meu_banco_horas GET`. "Saldo nao pode ser apagado manualmente" garantido pelo proprio design: nao existe endpoint de edicao ou exclusao de lancamento, e o saldo nunca e um campo gravado a parte — e sempre a soma calculada na consulta, entao nao ha nada que possa ser apagado. Saldo na rescisao: como o colaborador nunca e excluido ao ser desligado (so muda `status`), o extrato e o saldo continuam consultaveis normalmente apos o desligamento, sem necessidade de logica adicional. GAP CONSCIENTE: "limites" e "compensacao" (prazo maximo para compensar, teto de saldo) nao sao aplicados automaticamente — a tabela `regra_contrato.prazo_compensacao_banco_horas` existe mas o endpoint de lancamento nao valida `data_expiracao` contra esse prazo nem bloqueia lancamentos acima de um limite; ficaria a cargo do motor de regras dos itens 4.11-4.13, ainda nao implementado. Auditado. Publicado e verificado por diff pos-push.)
- [x] 4.15 Implementar simulador sem efeitos reais para jornada, saldo, ferias, impacto e inconsistencias; verificar comparacao entre regra atual e nova sem alterar dados de producao. (Novo endpoint `regras_override/{id}/simular GET` (RH/ADMIN): dado um regra_override (rascunho ou pendente), levanta a populacao de colaboradores afetados pela sua abrangencia (empresa/estabelecimento = todos os ativos; estado/municipio = por endereco pessoal; tipo_contrato/departamento/cargo/colaborador = filtro direto) e, para cada um, resolve o valor atualmente vigente do mesmo parametro via `resolver_regra`, comparando com o valor proposto (`representa_mudanca` bool) e sinalizando se ja ha conflito hoje (`conflito_atual`). Nao alarma nem persiste nada — so leitura, mais um registro de auditoria da propria simulacao (a simulacao em si, nao o override). Simplificacao: nao tenta prever se o override simulado *venceria* a resolucao (exigiria re-simular a hierarquia inteira assumindo esse candidato ja vigente); mostra o "antes" real e o "depois" proposto lado a lado, deixando a decisao de precedencia para o RH.)
- [x] 4.16 Implementar validadores e campos dos tres identificadores do Sistema Mediador/MTE, com exigencias por tipo de instrumento e referencia de termo aditivo; verificar formatos e regras de obrigatoriedade. (Campos ja existiam em `instrumento_normativo`. Concluido nesta sessao: nova function `ConectaHR/validar_formato_mte` — sem regex (confirmado nao funcionar neste workspace, mesma limitacao de `validar_cpf`), validando posicao a posicao via `split:""`+`foreach`: `numero_solicitacao_mediador` (MR + 5-6 digitos + / + 4 digitos), `numero_registro_mte` (2 letras maiusculas + 6 digitos + / + 4 digitos), `numero_processo_mte` (5 digitos + . + 6 digitos + / + 4 digitos + - + 2 digitos). Testada com 10 casos reais via `xano function run` (validos e invalidos dos 3 formatos) — todos com o resultado esperado. Conectada em `instrumentos_normativos POST`, validando cada campo quando informado (sao opcionais no cadastro inicial, exigidos so na aprovacao de instrumento coletivo, ja implementado — ver 4.17). Nao existe endpoint de edicao desses campos apos a criacao, entao este e o unico ponto de entrada que precisa validar.)
- [x] 4.17 Implementar ativacao condicionada de instrumentos coletivos, simulacao, versionamento e preservacao da regra aplicada; verificar que instrumento sem registro, documento ou aprovacao permanece pendente. (Confirmado que a ativacao condicionada ja estava implementada: `instrumentos_normativos/id/aprovar_POST` bloqueia a transicao para `vigente` de instrumentos coletivos sem os 3 identificadores MTE + data de registro + documento_url, mantendo pendente. Versionamento de `regra_override` tambem ja implementado (e com o bug de escopo corrigido nesta mesma sessao, auditoria anterior). Simulacao e preservacao da regra aplicada agora cobertas pelos novos endpoints das tarefas 4.12/4.15 (`regras_override/aplicar` e `regras_override/{id}/simular`), que funcionam para qualquer regra_override, inclusive as vinculadas a instrumento coletivo.)

## 5. Documentos e notificacoes

- [x] 5.1 Implementar metadados de documentos e armazenamento privado de anexos com validacao de tipo, tamanho, hash e validade; verificar upload, download autorizado e bloqueio de arquivo invalido. (Upload/metadados/validade/tipo ja implementados. Concluido nesta sessao: hash de duplicidade para `arquivo_url` (testado empiricamente que `|hash:"sha256"` NAO existe neste workspace — "Unable to locate func entry: hash" — mas `|md5` funciona, confirmado via `xano function run`; `documentos POST` agora calcula `hash_arquivo = arquivo_url|md5` e bloqueia cadastro duplicado do mesmo link para o mesmo colaborador). Gap documentado conscientemente: hash de conteudo real para `imagem_frente`/`imagem_verso` e validacao declarativa de tamanho/mime nao sao alcancaveis a partir do parametro `image` de entrada neste XanoScript — exigiria acesso aos bytes do arquivo (nao exposto) ou configuracao de campo pela UI do Xano (fora do alcance dos arquivos `.xs`); a quarentena completa por estados (enviado/em_verificacao/liberado/bloqueado) fica para a tarefa 5.7, nao pedida nesta rodada.)
- [x] 5.2 Implementar `documento.status` e as transicoes de analise, aprovacao, rejeicao, vencimento, substituicao e arquivamento; verificar que documentos vencidos permanecem armazenados e deixam de ser validos. (Confirmado nesta sessao que ja estava completo: `documentos/{id}/aprovar`, `.../rejeitar`, `.../arquivar`, substituicao via `documento_substituido_id` e a transicao `aprovado -> vencido` via `documentos/processar_vencimentos` (acionamento manual do RH, item 5.3) ja cobrem todas as transicoes do enum. Nenhum codigo novo necessario.)
- [x] 5.3 Implementar acionamento manual pelo RH para transicionar `aprovado -> vencido` e alertas em 30, 15 e 7 dias e no dia do vencimento; verificar idempotencia, fuso horario e que o colaborador continua ativo. (Implementado e publicado nesta sessao: campo `documento.ultimo_alerta_dias` (guarda o menor limiar 30/15/7/0 ja alertado) e endpoint `documentos/processar_vencimentos POST` (RH/ADMIN), que percorre documentos `aprovado` com `data_validade` definida, transiciona para `vencido` os que ja passaram da data e envia o alerta de e-mail correspondente (30/15/7/0 dias) aos que se aproximam, sem repetir o mesmo limiar em execucoes futuras — idempotente por documento. Mesma decisao de plataforma da tarefa 3.8: o plano Xano nao inclui Background Tasks e o upgrade foi recusado, entao "rotina diaria" virou acionamento manual do RH; spec.md e design.md atualizados para refletir isso como o comportamento definitivo, nao um contorno temporario. Publicado no Xano e verificado por diff pos-push sem diferencas de conteudo (so reformatacao cosmetica do servidor). Nota de plataforma nova: `var $nome {...}` (declaracao nova, nao `var.update`) dentro de um terceiro nivel de `conditional { if {} }` aninhado causou "Unknown variable" no linter e falha real no push (`Syntax error: unexpected '{'`) mesmo com `--dry-run` limpo — resolvido achatando a estrutura para no maximo dois niveis de conditional aninhado e usando ternarios (`? :`) para as duas variantes de mensagem em vez de duplicar o bloco `api.request`. Nao exercitado via HTTP real (dependeria de documentos de teste com `data_validade` proxima, o que o projeto pede para evitar) nem enviado de fato por e-mail (SENDGRID_API_KEY ainda nao configurada, mesma pendencia da tarefa 2.4).)
- [x] 5.4 Implementar solicitacao de documentos pendentes por RH, prazo e estado de atendimento; verificar criacao de pendencia para documento ausente ou vencido. (Implementado e publicado nesta sessao: tabela `pendencia_documento` (colaborador_id, tipo_documento, prazo, status `pendente`/`atendida`/`cancelada`, solicitado_por_user_id, atendida_por_documento_id); `pendencias_documento POST` (RH/ADMIN, bloqueia pendencia duplicada em aberto do mesmo tipo, notifica o colaborador por e-mail em melhor esforco); `pendencias_documento GET` (RH/ADMIN) e `minhas_pendencias_documento GET` (colaborador). Fechamento automatico: `documentos POST` agora localiza uma pendencia aberta do mesmo tipo ao receber um novo documento do colaborador e a marca `atendida` na mesma transacao, vinculando o documento que a resolveu. Auditado. Publicado e verificado por diff pos-push. E-mail de notificacao nao testado de ponta a ponta (mesma pendencia de SENDGRID_API_KEY das tarefas 2.4/5.3).)
- [x] 5.5 Implementar outbox, consumidor SendGrid, retentativas e chave de idempotencia; verificar falha do provedor, reprocessamento e ausencia de notificacao duplicada. (Nota anterior desatualizada — as integracoes de e-mail existiam, mas so sincronas para login/senha, que continuam corretas (usuario esperando na hora). Nova tabela `email_outbox` (status pendente/enviado/falhou, tentativas/max_tentativas, ultimo_erro, `chave_idempotencia` com indice unico) e endpoint `email_outbox/processar POST` (RH/ADMIN, acionamento manual — mesmo padrao de desligamento/vencimento, sem Background Tasks): envia pendentes via SendGrid, incrementa tentativas e registra o erro em falha, marca `falhou` ao esgotar `max_tentativas`, nunca reenvia o que ja esta `enviado`. `documentos/processar_vencimentos` migrado do envio sincrono direto para gravar no outbox (ganha retentativa, que o envio direto anterior nao tinha).)
- [x] 5.6 Implementar templates de notificacao para pendencias, decisoes e prazos sem dados sensiveis; verificar entrega e conteudo minimo em ambiente de teste. (Templates de texto simples (nao SendGrid Dynamic Template configuravel) embutidos em cada ponto de criacao de `email_outbox`/`notificacao_interna` — ferias aprovadas, documento vencendo, avaliacao disponivel, solicitacao respondida (ver 5.11). Mensagens genericas de proposito, sem dados sensiveis (sem salario, sem conteudo de avaliacao, sem motivo medico). Nao ha endpoint de gestao de templates (CRUD) — nada no spec pede templates editaveis pelo RH, escopo deliberadamente enxuto.)
- [x] 5.7 Implementar quarentena e verificacao de arquivos antes da liberacao; verificar extensao, tipo real, tamanho, hash, duplicidade, corrupcao e bloqueio de suspeitos. (Implementado e publicado nesta sessao: nova function `ConectaHR/verificar_arquivo_documento`, chamada por `documentos POST` (na criacao) e `documentos/{id} PATCH` (quando um novo `arquivo_url` e enviado), que preenche `estado_verificacao`/`motivo_bloqueio`. Verifica (1) extensao contra uma lista de permitidas (pdf/jpg/jpeg/png/doc/docx/xls/xlsx, extraida do fim da URL via `split:"."`+`last`) — bloqueia qualquer outra, incluindo extensoes executaveis; (2) tamanho maximo de 10MB e (3) Content-Type declarado pelo servidor remoto, ambos via um HEAD HTTP real (`api.request`) feito so quando a extensao ja passou. Hash (`|md5`) e duplicidade por `arquivo_url` ja existiam da tarefa 5.1 e continuam em `documentos POST`, sem alteracao. `documentos/{id}/aprovar` agora bloqueia (`accessdenied`, expondo o motivo) a aprovacao de um documento com `estado_verificacao == "bloqueado"`. Verificado ao vivo com token real: extensao `.exe` -> `estado_verificacao: "bloqueado"` com motivo, aprovacao desse documento corretamente negada; PDF real (URL publica) -> `estado_verificacao: "liberado"`, aprovacao bem-sucedida; PATCH substituindo o arquivo `.exe` por um PDF valido reprocessou e mudou o documento para `liberado`. Gap conscientemente aceito e documentado no cabecalho da function (mesma limitacao ja registrada na tarefa 5.1): XanoScript nesta plataforma nao da acesso aos bytes reais de um arquivo referenciado por `arquivo_url` externa, entao "tipo real" e "corrupcao" ficam limitados ao que o HEAD HTTP relata (Content-Type/Content-Length declarados pelo servidor remoto, nao o conteudo do arquivo em si) — verificacao de conteudo binario real exigiria baixar e inspecionar o arquivo, fora do alcance razoavel desta camada. Upload direto via campo `image` (`imagem_frente`/`imagem_verso`) e tratado como liberado por construcao, ja que o proprio Xano ja valida que e uma imagem real no momento do upload. Falha de rede/servidor inacessivel no HEAD (ex.: `arquivo_url` fora do ar) NAO bloqueia automaticamente — decisao deliberada para evitar falso-positivo por instabilidade de terceiros; o documento fica `liberado` sem a confirmacao extra de tamanho/tipo. Escopo mantido em `documento` (unica tabela com o campo `estado_verificacao`); `instrumento_normativo` nao tem esse campo e ficaria fora desta tarefa sem uma migracao de schema separada, nao pedida aqui.)
- [x] 5.8 Implementar matriz de documentos obrigatorios e politica de retencao parametrizada; verificar aplicabilidade, prazo, validade, anonimização, eliminacao/revisao e bloqueio por processo. (Nova tabela `documento_obrigatorio_regra`: aplicabilidade por tipo_contrato/cargo/departamento/idade (calculada de `data_nascimento`), mais `nacionalidade`/`condicao_profissional` armazenados mas nao casados automaticamente — colaborador nao tem esses campos, mesmo gap ja documentado em `resolver_regra`; politica de retencao completa (finalidade, base legal, prazo, evento inicial, tratamento: anonimizar/eliminar_automatico/revisao_manual/bloqueio_processo). Endpoints: `documentos_obrigatorios POST`/`GET` (RH/ADMIN cadastra/lista regras) e `colaboradores/{id}/documentos_pendentes_obrigatorios GET` (RH/ADMIN ou o proprio colaborador — casa as regras aplicaveis contra os documentos `aprovado` existentes e lista o que falta, sinalizando `aplicabilidade_incerta` quando a regra depende de nacionalidade/condicao_profissional). Aplicacao automatica da politica de retencao (eliminacao/anonimizacao real de documentos vencidos ha muito tempo) nao foi implementada — ficaria mais um acionamento manual do RH no mesmo padrao dos demais, nao pedido nesta rodada; os dados para isso ja estao todos modelados na tabela.)
- [x] 5.9 Implementar controles de minimizacao, finalidade, acesso por necessidade e protecao reforcada para documentos medicos, biometricos e sindicais; verificar que gestores nao visualizam diagnosticos. (Verificado nesta sessao, sem novo codigo: `grep` por "GESTOR" em todo `xano-workspace/api/conecta_rh_documentos` nao retorna nenhum resultado — nenhum endpoint de documento (`documentos GET`, `documentos/{id}/arquivo GET`, `documentos/{id} GET`, `meus_documentos GET`, etc.) concede acesso ao perfil Gestor; o modelo de acesso e sempre RH/ADMIN ou o proprio colaborador dono do documento. Isso ja satisfaz estruturalmente "gestores nao visualizam diagnosticos" — nao ha o que visualizar, pois Gestor nao acessa documento nenhum, sensivel ou nao, aso_admissional/laudo_deficiencia inclusive. Dados biometricos e filiacao sindical nao sao coletados em nenhum campo do MVP atual — nao ha o que proteger porque a feature simplesmente nao existe ainda (nao e uma lacuna deste item, e uma decisao de escopo do proprio design). Minimizacao e finalidade ja seguidas pelo padrao geral: cada endpoint retorna so os campos relevantes ao seu proposito (ex.: `output = [...]` explicito em varios `db.get`), sem um mecanismo formal e separado de "finalidade declarada" por campo.)
- [x] 5.10 Implementar metadados de ASO, PCMSO e eventos de SST sem armazenar prontuario medico como documento comum; verificar acesso restrito e exibicao operacional de apto/inapto quando aplicavel. (Implementado e publicado nesta sessao: tabela `evento_sst`, deliberadamente separada de `documento` — so guarda `resultado` operacional (`apto`/`inapto`/`apto_com_restricao`), tipo, datas, medico responsavel e uma observacao curta (max 300 caracteres, sem campo de diagnostico ou historico clinico em texto livre); `eventos_sst POST` (RH/ADMIN); `colaboradores/{id}/eventos_sst GET` (RH/ADMIN ou o proprio colaborador — sem Gestor, mesmo padrao confirmado no item 5.9). Publicado e verificado por diff pos-push.)
- [x] 5.11 Implementar central de notificacoes internas no proprio sistema (alem do envio por e-mail), marcavel como lida, para ferias aprovadas, documentos vencendo, avaliacao disponivel e solicitacoes respondidas; verificar que a notificacao interna e criada junto com o evento que dispara o e-mail. (Nova tabela `notificacao_interna` (tipo, titulo, mensagem, recurso, registro_id, lida/lida_em) e endpoints `minhas_notificacoes GET` (com contagem de nao lidas) e `notificacoes/{id}/marcar_lida POST` (so o proprio destinatario). Conectada nos 4 eventos exatos pedidos: `ferias/id/aprovar`, `documentos/processar_vencimentos` (por limiar de alerta), `avaliacoes POST` (ao atribuir), `solicitacoes/id/atender` e `.../indeferir`. Em cada um, a notificacao interna e o registro em `email_outbox` sao criados na mesma transacao/bloco de codigo, literalmente "juntos" como pedido. Push real feito e verificado por pull-diff em todos os 5 arquivos tocados.)
- [x] 5.12 Implementar tipos `holerite` e `informe_rendimentos` em `documento.tipo`, com criacao direta como `aprovado` quando RH anexa para o colaborador; verificar que o colaborador consegue baixar e que RH continua sendo o unico que pode anexar esses tipos. (Implementado e publicado nesta sessao: enum `documento.tipo` estendido com os dois valores; `documentos POST` passa a calcular `status_inicial` (`aprovado` para esses dois tipos, `pendente_analise` para os demais, como ja era) e `data_validade_final` (sempre `null` para esses tipos, ja que RH e a fonte da informacao e eles nao vencem); nova precondition bloqueia colaborador anexando holerite/informe_rendimentos para si mesmo — exclusivo de RH/ADMIN, mesmo quando o colaborador tem acesso de propriedade sobre o proprio cadastro de documentos. Download reaproveita a infraestrutura ja existente (`documentos/{id}/arquivo GET`, `meus_documentos GET`) sem nenhuma alteracao — RH/ADMIN e o proprio colaborador ja tinham acesso a qualquer tipo de documento pertencente ao colaborador. Publicado e verificado por diff pos-push.)

## 6. Avaliacao e desenvolvimento

- [x] 6.1 Implementar ciclos de avaliacao, competencias, avaliacao e respostas; verificar ciclo ativo, prazos, estados e permissao dos participantes. (Tabelas `avaliacao`, `ciclo_avaliacao`, `competencia_avaliacao` e `resposta_avaliacao` ja existiam; corrigido em sessao anterior `avaliacao.ciclo_avaliacao_id` e o default de `avaliacao.status`. Implementado e publicado nesta sessao: `ciclos_avaliacao POST/GET` (RH/ADMIN cria, qualquer um consulta); `avaliacoes POST` (RH/ADMIN atribui quem avalia quem dentro de um ciclo, com `relacao_avaliador`, bloqueia autoavaliacao com avaliador diferente do proprio colaborador); `minhas_avaliacoes GET` (fila do avaliador); `avaliacoes/{id}/respostas POST` (avaliador registra a nota por competencia, upsert — chamar de novo para a mesma competencia atualiza em vez de duplicar, aproveitando o indice unico ja existente); `avaliacoes/{id}/enviar POST` (calcula a nota geral como media das respostas, marca `enviada` — decisao imutavel, nao pode ser respondida de novo); `avaliacoes/{id} GET` (avaliador sempre, avaliado somente apos `enviada` para nao expor rascunho, RH/ADMIN sempre). Auditado (atribuicao e envio). Publicado e verificado por diff pos-push. Gap consciente: contestacao/revisao humana de avaliacao (parte do Requirement "Avaliacao justa e nao discriminatoria", tarefa 6.10) nao foi implementada aqui.)
- [x] 6.2 Implementar metas anuais com indicador, valor esperado, prazo, progresso, check-in, resultado e comentarios; verificar atualizacao de progresso e conclusao. (Implementado e publicado nesta sessao: `metas POST` (colaborador cria a propria, RH/ADMIN/Gestor podem criar para outro colaborador); `metas/{id}/checkin POST` (dono da meta ou RH/ADMIN, atualiza progresso/comentario/data); `metas/{id}/concluir POST` (RH/ADMIN ou Gestor da equipe, grava resultado final); `minhas_metas GET`. Publicado e verificado por diff pos-push. Ver 6.6 para o historico completo de check-ins, ainda nao implementado — esta versao guarda so o check-in mais recente, como ja modelado em `meta_avaliacao`.)
- [x] 6.3 Implementar PDI e acoes de desenvolvimento com responsavel, periodo, progresso e evidencia; verificar consulta pelo colaborador e gestor autorizado. (Implementado e publicado nesta sessao: `pdi POST` (RH/ADMIN/Gestor cria, com responsavel definido); `pdi/{id}/progresso POST` (o proprio colaborador, o responsavel, ou RH/ADMIN atualizam progresso e evidencia — conclui automaticamente ao atingir 100%); `meu_pdi GET`. Publicado e verificado por diff pos-push.)
- [x] 6.4 Implementar feedback privado gestor-colaborador e feedback publico entre colaboradores; verificar isolamento do privado e moderacao auditada do publico. (Implementado e publicado nesta sessao, seguindo a decisao ja registrada em design.md (reaproveitar `reconhecimento`, sem entidade nova): `reconhecimentos POST` calcula `visibilidade` automaticamente pela relacao remetente-destinatario (Gestor do departamento do destinatario -> `privado`; colega -> `publico`), sem deixar o remetente escolher; `meus_reconhecimentos_recebidos GET` (destinatario ve tudo que recebeu, publico ou privado — isolamento garantido porque nenhum outro endpoint expoe um registro privado a terceiros); `mural_reconhecimento GET` (feed publico, nunca lista privados). Publicado e verificado por diff pos-push.)
- [x] 6.5 Implementar reunioes individuais com assuntos, acordos, acoes, responsaveis, prazos, proxima reuniao e visibilidade; verificar acesso e historico. (Implementado e publicado nesta sessao: tabela `reuniao_individual` (assuntos, acordos, acoes, responsavel_acoes, prazo_acoes, proxima_reuniao, visibilidade `privado`/`compartilhado_rh`); `reunioes_individuais POST` (Gestor registra com colaborador da propria equipe, ou RH/ADMIN); `colaboradores/{id}/reunioes_individuais GET` — o colaborador ve as proprias, o Gestor autor ve as que registrou, RH/ADMIN veem somente as marcadas `compartilhado_rh` (filtragem por item, defesa em profundidade alem da checagem de perfil na entrada). Publicado e verificado por diff pos-push.)
- [x] 6.6 Implementar check-ins de metas com comentarios, dificuldades, evidencias, alteracao de prazo e historico; verificar trilha de mudancas. (Implementado e publicado nesta sessao: tabela `meta_checkin` (append-only, um evento por chamada, nunca sobrescrito) com progresso, comentario, dificuldades, evidencia e novo_prazo; `metas/{id}/checkin POST` estendido para aceitar esses campos e gravar a mudanca de prazo, atualizando `meta_avaliacao` (ultimo check-in, para consulta rapida) e `meta_checkin` (trilha completa) na mesma transacao; `metas/{id}/checkins GET` (dono da meta, Gestor da equipe, ou RH/ADMIN) retorna o historico completo em ordem cronologica. Publicado e verificado por diff pos-push.)
- [x] 6.7 Separar reconhecimento publico positivo de feedback corretivo privado; verificar competencia relacionada, moderacao, visibilidade e bloqueio de conteudo corretivo publico. (Implementado nesta sessao junto com 6.4: `reconhecimentos POST` so grava como `privado` quando o remetente e comprovadamente o Gestor do departamento do destinatario — nunca por escolha livre do remetente, o que ja bloqueia estruturalmente conteudo corretivo entrando como publico via API. `reconhecimentos/{id}/moderar POST` (RH/ADMIN) cobre o caso de um publico que se revele nao-positivo apos publicado — marca `moderado` com motivo auditado, sem apagar o registro. GAP CONSCIENTE: nao ha validacao automatica de "conteudo e positivo" no momento do envio (isso exigiria analise de sentimento/NLP, fora do escopo) — a garantia e estrutural (quem pode gravar privado) mais moderacao humana a posteriori para o publico, nao um filtro de conteudo em tempo real.)
- [x] 6.8 Implementar pesquisa anonima de clima com resultados agrupados e quantidade minima de respostas; verificar anonimato e supressao de grupos pequenos. (Implementado e publicado nesta sessao: `pesquisa_clima` (titulo, vigencia, `minimo_respostas`), `pergunta_clima`, `resposta_clima` (SEM colaborador_id/user_id nenhum — so `departamento_id`, capturado no momento da resposta so para permitir agrupamento; nao ha como ligar uma resposta a uma pessoa, nem para RH/ADMIN via banco) e `resposta_clima_participacao` (tabela separada que so guarda quem-ja-respondeu-qual-pergunta, para bloquear resposta duplicada, nunca cruzada com o conteudo da resposta). `pesquisas_clima POST/GET`, `pesquisas_clima/{id}/perguntas POST`, `perguntas_clima/{id}/responder POST` (bloqueia segunda resposta do mesmo colaborador a mesma pergunta), `pesquisas_clima/{id}/resultados GET` (RH/ADMIN): calcula quantidade e media por departamento e o geral da empresa, para cada pergunta, e SO inclui um grupo no resultado quando a quantidade de respostas atinge `minimo_respostas` — abaixo disso, o grupo simplesmente nao aparece (supressao). Publicado e verificado por diff pos-push, inclusive o endpoint de resultados com 4 niveis de `foreach` aninhado, que passou limpo na primeira tentativa.)
- [x] 6.9 Implementar matriz de competencias e plano de carreira sem promocao automatica; verificar cargo, nivel, departamento, lacunas, metas, PDI e historico de evolucao. (Implementado e publicado nesta sessao: `colaboradores/{id}/plano_carreira GET` (o proprio colaborador, RH/ADMIN, ou Gestor da equipe) — agrega nivel atual, proximo nivel (calculado, l1->l2->...->l5, nunca gravado/promovido automaticamente), competencias esperadas no proximo nivel via `competencia_avaliacao` (lacunas a desenvolver), metas ativas, PDI ativo e historico de evolucao via `historico_profissional`. Tudo consulta pura sobre dados ja existentes, nenhuma tabela nova. Publicado e verificado por diff pos-push. Gap consciente: "lacunas" e a lista de competencias esperadas do proximo nivel, nao um calculo numerico de gap contra notas de avaliacao (cruzar avaliacao->resposta_avaliacao->competencia por colaborador para uma pontuacao de lacuna e um escopo maior, nao tentado aqui).)
- [x] 6.10 Implementar criterios de avaliacao previamente informados, contestacao e revisao humana; verificar bloqueio de discriminacao e de decisoes automaticas de promocao, punicao ou desligamento. (Criterios previamente informados: ja garantido pelo desenho do item 6.1 — `avaliacoes POST` exige uma `competencia_avaliacao` ja cadastrada por RH antes de qualquer avaliacao poder referencia-la, entao o criterio sempre existe e e consultavel antes da avaliacao acontecer. Bloqueio de decisoes automaticas: nao existe, em nenhum lugar do codigo, uma rotina que leia `avaliacao`/`resposta_avaliacao` e altere `colaborador.nivel`, crie um desligamento ou dispare qualquer efeito de RH automaticamente — toda mudanca de nivel/cargo/desligamento passa pelos fluxos proprios (`vinculo PATCH`, `solicitacoes_desligamento`), sempre com decisao humana explicita. Implementado nesta sessao: tabela `contestacao_avaliacao`; `avaliacoes/{id}/contestar POST` (o colaborador avaliado contesta uma avaliacao `enviada`, sem alterar a avaliacao original); `contestacoes_avaliacao GET` (RH/ADMIN) e `contestacoes_avaliacao/{id}/revisar POST` (RH/ADMIN registra a resposta da revisao humana). Auditado. Publicado e verificado por diff pos-push. Gap consciente: bloqueio ativo de criterios discriminatorios (o cenario "Criterio discriminatorio" da spec, sobre impedir usar atributo protegido em ranking/recomendacao) nao foi implementado porque nao existe hoje nenhum endpoint de ranking ou recomendacao automatizada no codigo — nao ha o que bloquear ainda.)

## 7. Integracao e entrega

- [ ] 7.1 Criar telas e endpoints dos fluxos por fatia vertical, aplicando estados de carregamento, erro, sucesso e acesso negado; verificar os principais caminhos por perfil.
- [ ] 7.2 Executar testes de integracao cobrindo autenticacao, autorizacao, ponto, ferias, documentos, e-mail e avaliacao; verificar isolamento entre departamentos e perfis.
- [ ] 7.3 Executar testes de seguranca para credenciais, tokens, codigo de acesso de login, uploads, logs e feedback privado; verificar que nenhum segredo ou documento e exposto.
- [ ] 7.4 Preparar deploy com feature flags, dados sinteticos e plano de rollback sem importar registros das imagens; verificar smoke test em ambiente controlado e registrar a configuracao de producao.
- [ ] 7.5 Realizar validacao inicial de mercado com usuarios/testadores, coletar feedback e medir uso basico; verificar relatorio de achados e backlog priorizado.
- [ ] 7.6 Preparar a entrega final com README, arquitetura, especificacoes OpenSpec, evidencias de testes e demonstracao; verificar que um avaliador consegue reproduzir o fluxo principal.
- [ ] 7.7 Implementar painel de indicadores e exportacoes CSV/PDF respeitando permissoes e auditoria; verificar colaboradores, ponto, ferias, ausencias, documentos, auditoria, avaliacoes, metas, PDIs, headcount, turnover, admissoes, desligamentos, absenteismo, distribuicao por departamento e horas extras.
- [ ] 7.8 Implementar preferencias de notificacao sem permitir desativar avisos obrigatorios de seguranca; verificar canais, lembrete diario, resumo semanal e tipos de evento.
- [ ] 7.9 Implementar sessoes e dispositivos, alertas de acesso suspeito, monitoramento operacional, backups e recuperacao; verificar encerramento seletivo, falhas de tarefas, filas, restauracao e acesso bloqueado.
- [ ] 7.10 Garantir acessibilidade do frontend Reflex e versionamento `/api/v1/`; verificar teclado, contraste, labels, foco, responsividade, textos alternativos, erros compreensiveis e compatibilidade da API.
- [ ] 7.11 Implementar auditoria obrigatoria para autenticacao, usuarios, contratos, ponto, documentos, regras, exportacoes e avaliacoes; verificar autor, data, justificativa, valores anterior/novo e resultado.
- [ ] 7.12 Documentar no README que o ponto do MVP e controle interno experimental e criar backlog de conformidade REP-P, REP-A e REP-C conforme Portaria nº 671/2021; verificar que a interface nao declara certificacao inexistente.
- [ ] 7.13 Criar prototipos navegaveis no Figma para login, central de pendencias, onboarding, ponto, ferias, documentos, auditoria e regras; verificar fluxos por perfil antes do desenvolvimento e consistencia no handoff para Reflex.
- [x] 7.14 Remover o grupo de API `leonardos_workspace` e o cluster de rotas `solicitacao_desligamento` dentro de `conecta_rh_colaboradores` (CRUD generico sem autenticacao nem autorizacao, incluindo sobre `solicitacao_desligamento`) antes da entrega; verificar que nenhuma rota exposta permite contornar as politicas de perfil e escopo. (CORRECAO nesta sessao: a marcacao anterior so refletia a remocao dos arquivos locais em `xano-workspace/api` — a recomendacao do proprio texto ["apos sincronizar com o Xano, conferir..."] nunca tinha sido seguida, e o grupo `leonardos_workspace` (86 endpoints CRUD sem auth sobre todas as 15 tabelas, incluindo `user` com PATCH/DELETE livres) continuava live no servidor. Confirmado lendo o codigo real (`api/leonardos_workspace/colaborador_GET.xs` sem `auth`) antes de agir. Removido de verdade nesta sessao: grupo `leonardos_workspace` e cluster `solicitacao_desligamento` apagados pelo usuario no Admin UI; endpoint orfao `documentos/{id}/status PATCH` (versao antiga, ja substituida por aprovar/rejeitar/arquivar) removido via `xano workspace push --sync --delete`; 5 functions leftover (`debug_timestamp`, `debug_api_request`, `debug_user_count`, `debug_reset_senha_teste`, e `emitir_otp_login` — morta, nunca chamada de fato) removidas pelo usuario. Validado por dry-run pos-limpeza: zero operacoes destrutivas pendentes.)

## 8. Experiencia e produtividade

- [x] 8.1 Implementar central de solicitacoes do colaborador (alteracao cadastral, declaracao, documento avulso, outra), com decisao do RH e agregacao de ferias/ausencia/correcao de ponto na mesma tela; verificar que aprovacao de alteracao cadastral nao altera o cadastro automaticamente. (Implementado e publicado nesta sessao: tabela `solicitacao_rh` (colaborador_id, tipo, descricao, status `recebida`/`em_analise`/`atendida`/`indeferida`, decisao); `solicitacoes POST` (colaborador cria), `solicitacoes GET` (RH/ADMIN listam todas), `solicitacoes/{id}/atender POST` e `solicitacoes/{id}/indeferir POST` (RH/ADMIN decidem, com justificativa obrigatoria no indeferimento). `minhas_solicitacoes GET` implementa a "central unificada": agrega as proprias `solicitacao_rh` com as proprias `ferias` e `ausencia` numa unica resposta, sem duplicar esses fluxos. Confirmado no proprio comentario do endpoint `atender`: atender uma solicitacao `alteracao_cadastral` so muda o status da solicitacao — o RH ainda precisa aplicar a mudanca pelo fluxo de cadastro existente (`colaboradores/{id} PATCH` ou `.../vinculo PATCH`) para o cadastro mudar de fato, preservando validacao e historico. Correcao de ponto (item 4.2) entra na agregacao quando existir — nao foi possivel incluir porque a feature nao esta implementada. Auditado. Publicado e verificado por diff pos-push.)
- [x] 8.2 Implementar organograma da empresa a partir dos dados de cargo, departamento e vinculo gestor-colaborador; verificar visualizacao hierarquica restrita a nome, cargo e departamento. (Implementado e publicado nesta sessao: `organograma GET`, aberto a qualquer usuario autenticado. Retorna tres listas planas (departamentos com `gestor_colaborador_id`, cargos, colaboradores ativos com `cargo_id`/`departamento_id`) para o consumidor montar a hierarquia por id, em vez de aninhar a estrutura no backend; nenhum campo sensivel (salario, CPF, contato) e retornado, so nome/cargo/departamento por pessoa. Publicado e verificado por diff pos-push.)
- [x] 8.3 Implementar tempo de empresa, aniversariantes do mes e timeline do colaborador a partir dos dados ja existentes (admissao, historico profissional, ferias e avaliacoes concluidas); verificar calculo e ordem cronologica. (Implementado e publicado nesta sessao: `tempo_empresa_dias` calculado a partir de `data_admissao` e adicionado a `meu_perfil_colaborador GET` e `colaboradores/{id} GET`; `colaboradores/aniversariantes GET` (aberto a qualquer usuario, retorna so nome e dia/mes — nunca o ano, para nao revelar idade); `colaboradores/{id}/timeline GET` (RH/ADMIN veem qualquer colaborador, o proprio colaborador ve a propria timeline, Gestor ve colaboradores do departamento que gerencia) reunindo `historico_profissional` e ferias com status `Concluida` em ordem cronologica. Gap consciente: a parte de "avaliacoes concluidas" nao entra porque o modulo de avaliacao (secao 6) nao esta implementado; a timeline ja esta preparada para incluir quando existir. Publicado e verificado por diff pos-push.)
- [x] 8.4 Implementar pesquisa global de colaboradores por nome, CPF, matricula, cargo e departamento; verificar que o resultado respeita o escopo de acesso do usuario. (Implementado e publicado nesta sessao: `colaboradores/buscar GET` (input `termo`), casando por nome (substring, via filtro `icontains`), CPF (igualdade exata, nunca retornado no resultado), matricula/id (igualdade exata) e nome de cargo/departamento (substring, resolvido antes da busca principal). Escopo: Gestor recebe somente colaboradores do departamento que gerencia (`departamento.gestor_colaborador_id`); RH, ADMIN e COLABORADOR recebem qualquer colaborador ativo — mesmo modelo de visibilidade aberta do organograma. Publicado e verificado por diff pos-push. Nota de plataforma nova, testada e confirmada nesta tarefa: o filtro `|push:item` (adicionar item ao fim de um array) funciona corretamente vindo de `var.update`, inclusive empurrando um objeto literal — usado tanto aqui quanto em 8.3 (aniversariantes) sem problema.)
- [x] 8.5 Implementar comunicados internos publicados por RH/Admin com publico-alvo e vigencia, exibidos no dashboard do colaborador; verificar expiracao automatica sem apagar o registro. (Implementado e publicado nesta sessao: tabela `comunicado` (titulo, conteudo, publico_alvo `todos`/`departamento`/`perfil`, departamento_id ou perfil_alvo conforme o publico, data_inicio, data_fim, ativo); `comunicados POST` (RH/ADMIN, valida a combinacao publico_alvo + departamento_id/perfil_alvo); `comunicados GET` (RH/ADMIN, gestao); `meus_comunicados GET` (qualquer usuario, filtra pela vigencia e pelo escopo — todos, ou so o proprio departamento, ou so o proprio perfil). "Expiracao automatica sem apagar o registro" e resolvida por consulta: `meus_comunicados` so retorna comunicados com `data_fim` nula ou futura, mas o registro nunca e apagado nem tem status alterado — comunicados expirados continuam em `comunicados GET` para historico do RH. Auditado (publicacao). Publicado e verificado por diff pos-push.)
- [x] 8.6 Implementar base de conhecimento (FAQ) do RH por categoria (ferias, ponto, documentos, politicas internas), mantida por RH/Admin; verificar consulta por todos os colaboradores autenticados. (Implementado e publicado nesta sessao: tabela `artigo_faq` (categoria, titulo, conteudo); `artigos_faq POST` (RH/ADMIN); `artigos_faq GET` (qualquer usuario autenticado, so ativos). Conteudo sobre beneficios fica fora do escopo, conforme o Non-Goal ja registrado em proposal.md. Publicado e verificado por diff pos-push.)
