## 1. Fundacao e ambiente

- [ ] 1.1 Criar e organizar o repositorio GitHub, README inicial, estrategia de branches e responsabilidades do grupo; verificar clone limpo, primeiro pull request e instrucoes de setup. (Feito nesta sessao: repositorio git local inicializado, README com stack/setup/estrategia de branches, .gitignore e primeiro commit. Falta: criar o repositorio remoto no GitHub (nao ha `gh` CLI autenticado neste ambiente), configurar o remote, abrir o primeiro Pull Request e preencher a tabela de responsabilidades do grupo.)
- [ ] 1.2 Definir a estrutura do aplicativo, configurar Reflex, ambientes e pipeline de validacao; verificar que build, lint e testes executam em ambiente limpo.
- [ ] 1.3 Criar migracoes do banco relacional para usuarios, colaboradores, cargos, departamentos, historico profissional e entidades funcionais; verificar chaves, indices, restricoes e rollback das migracoes. (Reaproveitar: 21 tabelas ja existem em xano-workspace/table com FKs e indices. Corrigido nesta sessao: `user.senha` (era max:12, agora max:64|min:8), `historico_profissional.tipo_contrato_` (era texto livre, agora enum `tipo_contrato` igual ao de `colaborador`), `ausencia.status` (removido o valor espurio "Afastamento") e `ausencia.tipo` ("Licença" padronizado para "Licenca", compativel com a validacao ja existente nos endpoints). Falta sincronizar com o Xano e confirmar que as migracoes aplicam sem quebrar dados existentes.)
- [ ] 1.4 Criar catalogos e validadores para todos os enums informados; verificar que valores validos sao aceitos e valores fora do dominio sao rejeitados.
- [ ] 1.5 Implementar auditoria estruturada para eventos sensiveis sem registrar segredos; verificar eventos de autenticacao, alteracoes e decisoes. (Tabela `auditoria` ja existe, mas nenhum endpoint grava nela ainda — login, troca de senha e decisoes nao sao auditados; falta implementar a escrita.)
- [ ] 1.6 Configurar a matriz inicial de jornada e ferias por contrato: CLT 8/44, PJ nao aplicavel, ESTAGIO 6/30, APRENDIZ 6/30, TEMPORARIO 8/44 e OUTRO manual; verificar calculos, elegibilidade, solicitacao e fracionamento com dados sinteticos por contrato. (Tabela `regra_contrato` ja tem os campos da matriz; falta confirmar se esta semeada com os valores iniciais e implementar a logica de calculo/elegibilidade.)
- [ ] 1.7 Criar a central de tarefas e pendencias por perfil; verificar documentos, aprovacoes, avaliacoes, metas, PDI, senha temporaria e 2FA pendentes em uma unica consulta.
- [x] 1.8 Implementar validador local de CPF com normalizacao, onze digitos, rejeicao de sequencias repetidas e dois digitos verificadores; verificar CPF valido e invalido sem chamada externa. (Implementado e testado nesta sessao em `xano-workspace/function/conectahr/validar_cpf.xs`, publicado no workspace Xano e validado com `xano function run` em 6 casos reais: CPF valido com e sem mascara, primeiro e segundo digito verificador invalidos, sequencia repetida e tamanho incorreto — todos com o resultado esperado. Nota de plataforma: `regex_replace`/`regex_get_all_matches` nao funcionam neste workspace Xano (retornam null/vazio mesmo com sintaxe documentada); a normalizacao usa `split:""` + `foreach` + comparacao literal em vez de regex. Falta conectar a function ao cadastro de colaborador (item 3.2).)
- [ ] 1.10 Criar catalogo de parametros protegidos como configuravel, configuravel com aprovacao ou sem override; verificar bloqueio de alteracoes indevidas pelo RH.
- [ ] 1.11 Criar no Figma o design system do ConectaRH com tokens, tipografia, espacamentos, grid, componentes e estados; verificar documentacao visual e criterios de acessibilidade.

## 2. Identidade e autorizacao

- [ ] 2.1 Implementar cadastro de usuario restrito a Admin e RH, hash de senha e estado de senha temporaria; verificar que outros perfis recebem acesso negado. (API `conecta_rh_gestao_de_usuarios` ja existe; verificar se o cadastro esta de fato restrito a Admin/RH antes de considerar concluido.)
- [ ] 2.2 Implementar login, token de uma hora, logout com revogacao e rejeicao de token expirado ou adulterado; verificar testes de ciclo completo de sessao. (Corrigido nesta sessao: `auth/login_POST` agora emite token de 3600s (1h) em vez de 24h. Ainda falta: tabela de sessoes e endpoint de logout — hoje nao ha como revogar um token emitido antes da expiracao.)
- [ ] 2.3 Implementar troca obrigatoria de senha temporaria e redefinicao por fluxo de uso unico; verificar que o primeiro acesso bloqueia modulos e que a resposta de recuperacao nao enumera contas. (Troca de senha ja implementada em `auth/senha_PATCH`; falta bloquear rotas funcionais enquanto `senha_primeiro_acesso` estiver ativo e falta o endpoint de redefinicao via e-mail.)
- [ ] 2.4 Implementar configuracao, desafio e recuperacao de 2FA TOTP; verificar login com codigo valido, codigo invalido, segredo expirado e codigo de recuperacao usado. (Nao iniciado — nenhum campo de TOTP em `user` nem endpoint relacionado.)
- [ ] 2.5 Implementar politica centralizada de autorizacao por perfil e escopo colaborador-gestor-departamento; verificar acesso permitido e negado em API, nao apenas na interface. (Cada endpoint em `conecta_rh_*` repete manualmente as checagens de perfil/escopo; funciona mas nao e centralizado — considerar extrair para uma function compartilhada.)

## 3. Cadastro organizacional

- [ ] 3.1 Implementar CRUD de cargos e departamentos com vinculos de gestores por vigencia; verificar troca, substituicao temporaria, ausencia de sobreposicao indevida e historico. (CRUD de cargos/departamentos existe; `departamentos/{id}/gestor_PATCH` apenas sobrescreve o gestor sem vigencia nem historico — precisa de tabela dedicada para preservar trocas anteriores.)
- [ ] 3.2 Implementar cadastro e atualizacao de colaborador vinculado ao usuario, gestor e departamento; verificar validacao de contrato, nivel, status e consistencia dos relacionamentos. (API `conecta_rh_colaboradores` existe e cobre CRUD e vinculo; depende da validacao de CPF do item 1.8, ainda nao implementada.)
- [ ] 3.3 Implementar historico profissional transacional para admissao, promocao, alteracoes e desligamento; verificar preservacao dos valores anteriores e responsavel auditado. (Reaproveitar: `colaboradores/{id}/vinculo_PATCH` ja implementa a transacao completa para admissao/promocao/alteracoes, encerrando o historico anterior e criando um novo registro. Corrigido nesta sessao: o campo `tipo_contrato_` da tabela tinha nome e tipo incompativeis com o que esse endpoint ja gravava — agora e um enum `tipo_contrato` igual ao de `colaborador`. Falta apenas o desligamento (3.7/3.8) gravar seu proprio evento neste historico.)
- [ ] 3.4 Implementar onboarding com checklist de admissao, documentos obrigatorios, configuracoes e acompanhamentos de 30, 60 e 90 dias; verificar progresso, pendencias e acesso por perfil.
- [ ] 3.5 Implementar validacoes especificas de admissao CLT e estados de comunicacao eSocial/CTPS Digital; verificar bloqueio sem registro e controle dos prazos aplicaveis.
- [ ] 3.6 Implementar modelos contratuais para CLT, estagio, aprendiz, temporario e PJ; verificar termo de compromisso, programa de aprendizagem, empresa/tomadora, contrato escrito e ausencia de presuncao automatica de vinculo.
- [ ] 3.7 Implementar o fluxo de solicitacao, analise e decisao de desligamento (imediato e aviso previo) por Colaborador ou Gestor dentro do proprio escopo, com aprovacao exclusiva do RH; verificar bloqueio de solicitacoes duplicadas, desativacao do acesso e atualizacao do status do colaborador. (Reaproveitar: `conecta_rh_desligamentos` ja implementa criar/iniciar_analise/aprovar/rejeitar/cancelar com essas regras. Corrigido nesta sessao: a checagem morta de status "aprovado" em `solicitacoes_desligamento_POST` — que nunca era alcancada, pois `aprovar_POST` grava "concluido" ou "agendado" — foi substituida por uma checagem real do estado "em_analise", que antes nao bloqueava solicitacao duplicada. Removido tambem um segundo cluster de rotas `solicitacao_desligamento` auto-geradas e sem autenticacao dentro de `conecta_rh_colaboradores` que coexistia com este fluxo — ver 7.14.)
- [ ] 3.8 Registrar o desligamento concluido no historico profissional (`tipo_alteracao = desligamento`) e implementar a rotina diaria que conclui desligamentos `agendado` quando a `data_efetiva` chegar; verificar idempotencia e que nenhum colaborador e reativado indevidamente. (Nao iniciado — `aprovar_POST` atualiza `colaborador.status` mas nao cria o evento de historico, e nao existe rotina para concluir desligamentos agendados, equivalente a rotina de vencimento de documentos do item 5.3.)

## 4. Jornada e ausencias

- [ ] 4.1 Implementar marcadores de ponto, calculo de horas e estados ABERTO, COMPLETO, INCOMPLETO e AJUSTADO; verificar jornadas completas, incompletas e calculo de extras parametrizavel. (Implementado e publicado nesta sessao: grupo `ConectaRH - Ponto` com `ponto/marcar` (auto-servico; progride entrada -> inicio intervalo -> fim intervalo -> saida, criando o registro do dia na primeira marcacao e calculando `horas_trabalhadas` na saida), `meu_ponto` (espelho do colaborador) e `ponto` (consulta por RH/ADMIN). Aritmetica de timestamp (subtracao direta, ja que sao inteiros em ms) e formatacao de data validadas com `xano function run` antes de escrever o endpoint. Nao foi exercitado via HTTP com dados reais — exigiria criar colaborador/usuario de teste, o que o projeto pede para evitar; validado por push limpo. Falta: calculo de `horas_extras` parametrizado por contrato (depende do motor de regras dos itens 4.4/4.13, ainda nao implementado) e o status INCOMPLETO/AJUSTADO (hoje so ABERTO e COMPLETO sao usados; INCOMPLETO exigiria fechamento automatico de dias sem saida registrada, AJUSTADO faz parte da correcao de ponto do item 4.2).)
- [ ] 4.2 Implementar solicitacao e decisao de correcao de ponto com justificativa, preservacao do original e idempotencia; verificar aprovacao, recusa e acesso restrito ao responsavel. (Nao iniciado.)
- [ ] 4.3 Implementar solicitacao de ferias, validacao de periodo e decisoes de RH/Gestor; verificar transicoes Pendente, Aprovada, Rejeitada, Cancelada e Concluida. (Reaproveitar: `conecta_rh_ferias` ja implementa solicitar, aprovar, rejeitar e cancelar; falta validar contra a matriz de regras do item 4.4 e cobrir com testes.)
- [ ] 4.4 Aplicar as regras contratuais de ferias, incluindo recesso de estagio, proporcionalidade, bloqueio de solicitacao para PJ e temporario por padrao e limite de fracionamento; verificar cada linha da matriz e registrar a fonte normativa da CLT. (Schema de `regra_contrato` cobre os campos da matriz; a validacao das regras por contrato ainda nao esta amarrada aos endpoints de ferias.)
- [ ] 4.5 Implementar registro e consulta de ausencias por tipo e status; verificar tipos permitidos, periodo valido, escopo de acesso e transicoes de decisao. (Reaproveitar: `conecta_rh_ausencias` ja implementa registrar, aprovar, rejeitar e registrar (confirmar administrativo), cobrindo os quatro status da spec. Corrigido nesta sessao: enum `ausencia.status` (removido o valor invalido "Afastamento") e `ausencia.tipo` (acento padronizado). Falta apenas rodar os testes de ponta a ponta.)
- [ ] 4.6 Implementar delegacao temporaria de aprovacao com expiracao automatica e bloqueio de autoaprovacao; verificar titular, substituto, vigencia, permissoes e auditoria.
- [ ] 4.7 Implementar prazos, estados atrasados e escalonamento para responsavel e nivel superior; verificar notificacao e auditoria de cada escalonamento.
- [ ] 4.8 Implementar calendario organizacional e verificacao de conflitos de ferias; verificar feriados, dias nao uteis, sobreposicoes, saldo, equipe minima e alertas configuraveis.
- [ ] 4.9 Criar `instrumento_normativo` com tipos, status, documento comprobatório, vigencia e aprovação; verificar bloqueio de autoaprovacao, expiracao, suspensao e revogacao. (Tabela ja implementada de forma proxima a spec, incluindo os tres identificadores do Sistema Mediador/MTE; falta a API de aprovacao/ativacao.)
- [ ] 4.10 Criar `regra_override` versionada com parametros, valores, prioridade, abrangencia e vigencia; verificar que regras vigentes nao sao sobrescritas. (Tabela ja implementada com versionamento, status e abrangencia; falta a API.)
- [ ] 4.11 Implementar resolução por ordem de especificidade e prioridade, bloqueio de conflitos e aprovação humana; verificar matriz, norma, instrumento coletivo, cargo/departamento e excecao individual. (Tabela `regra_aplicada` ja registra regra/versao/instrumento/parametros por calculo; falta a logica de resolucao em si.)
- [ ] 4.12 Implementar simulacao de impacto e persistencia da regra aplicada em calculos; verificar colaboradores afetados, versao, instrumento, parametros e data.
- [ ] 4.13 Implementar regras de escala, intervalos, descanso, adicional noturno e limites de horas extras; verificar bloqueio de matriz acima do limite sem override aprovado.
- [ ] 4.14 Implementar banco de horas com origem autorizadora, creditos, debitos, limites, compensacao, expiracao e saldo na rescisao; verificar que saldo nao pode ser apagado manualmente.
- [ ] 4.15 Implementar simulador sem efeitos reais para jornada, saldo, ferias, impacto e inconsistencias; verificar comparacao entre regra atual e nova sem alterar dados de producao.
- [ ] 4.16 Implementar validadores e campos dos tres identificadores do Sistema Mediador/MTE, com exigencias por tipo de instrumento e referencia de termo aditivo; verificar formatos e regras de obrigatoriedade. (Campos ja existem em `instrumento_normativo`; falta a validacao de formato via regex nos endpoints.)
- [ ] 4.17 Implementar ativacao condicionada de instrumentos coletivos, simulacao, versionamento e preservacao da regra aplicada; verificar que instrumento sem registro, documento ou aprovacao permanece pendente.

## 5. Documentos e notificacoes

- [ ] 5.1 Implementar metadados de documentos e armazenamento privado de anexos com validacao de tipo, tamanho, hash e validade; verificar upload, download autorizado e bloqueio de arquivo invalido. (Upload e metadados ja implementados em `conecta_rh_documentos`, incluindo `hash_arquivo`; falta quarentena e validacao de duplicidade antes da liberacao.)
- [ ] 5.2 Implementar `documento.status` e as transicoes de analise, aprovacao, rejeicao, vencimento, substituicao e arquivamento; verificar que documentos vencidos permanecem armazenados e deixam de ser validos. (Implementado e publicado nesta sessao: `documentos/{id}/aprovar`, `documentos/{id}/rejeitar` e `documentos/{id}/arquivar` (RH/ADMIN, respeitando as transicoes validas do enum), e `documentos POST` passou a aceitar `documento_substituido_id` para substituir um documento aprovado/vencido numa transacao. Corrigido tambem o enum `documento.tipo`, que nao batia com os valores ja validados pela API (`cin`, `aso_admissional`, `outro` minusculo). Seguem o padrao ja comprovado de ferias/ausencias/desligamento; validados por push limpo e diff, mas nao exercitados via HTTP com dados reais. O antigo `documentos/{id}/status` (que so alternava o campo `ativo`) foi removido do repositorio local, mas continua publicado no Xano ate ser apagado manualmente pelo Admin — nao ha como excluir um unico endpoint remoto pelo CLI sem arriscar o workspace inteiro (o `--delete` do Xano CLI ignora filtros de include/exclude e considera o workspace todo). Falta a transicao automatica `aprovado -> vencido` (item 5.3).)
- [ ] 5.3 Implementar tarefa diaria do Xano para transicionar `aprovado -> vencido` e alertas em 30, 15 e 7 dias e no dia do vencimento; verificar idempotencia, fuso horario e que o colaborador continua ativo. (Nao iniciado.)
- [ ] 5.4 Implementar solicitacao de documentos pendentes por RH, prazo e estado de atendimento; verificar criacao de pendencia para documento ausente ou vencido.
- [ ] 5.5 Implementar outbox, consumidor SendGrid, retentativas e chave de idempotencia; verificar falha do provedor, reprocessamento e ausencia de notificacao duplicada. (Nao iniciado — nenhuma integracao de e-mail encontrada no workspace.)
- [ ] 5.6 Implementar templates de notificacao para pendencias, decisoes e prazos sem dados sensiveis; verificar entrega e conteudo minimo em ambiente de teste.
- [ ] 5.7 Implementar quarentena e verificacao de arquivos antes da liberacao; verificar extensao, tipo real, tamanho, hash, duplicidade, corrupcao e bloqueio de suspeitos. (Campo `estado_verificacao` ja existe em `documento` com os quatro estados previstos; falta a logica de verificacao.)
- [ ] 5.8 Implementar matriz de documentos obrigatorios e politica de retencao parametrizada; verificar aplicabilidade, prazo, validade, anonimização, eliminacao/revisao e bloqueio por processo.
- [ ] 5.9 Implementar controles de minimizacao, finalidade, acesso por necessidade e protecao reforcada para documentos medicos, biometricos e sindicais; verificar que gestores nao visualizam diagnosticos.
- [ ] 5.10 Implementar metadados de ASO, PCMSO e eventos de SST sem armazenar prontuario medico como documento comum; verificar acesso restrito e exibicao operacional de apto/inapto quando aplicavel.

## 6. Avaliacao e desenvolvimento

- [ ] 6.1 Implementar ciclos de avaliacao, competencias, avaliacao e respostas; verificar ciclo ativo, prazos, estados e permissao dos participantes. (Tabelas `avaliacao`, `ciclo_avaliacao`, `competencia_avaliacao` e `resposta_avaliacao` ja existem. Corrigido nesta sessao: adicionado `avaliacao.ciclo_avaliacao_id` (nao existia) e o default de `avaliacao.status` (era "Rascunhho", com erro de digitacao e fora do enum permitido; agora "pendente"). Ainda falta a API propria no grupo `conecta_rh_*` — hoje nao ha nenhuma.)
- [ ] 6.2 Implementar metas anuais com indicador, valor esperado, prazo, progresso, check-in, resultado e comentarios; verificar atualizacao de progresso e conclusao. (Tabela `meta_avaliacao` ja cobre os campos; falta a API propria.)
- [ ] 6.3 Implementar PDI e acoes de desenvolvimento com responsavel, periodo, progresso e evidencia; verificar consulta pelo colaborador e gestor autorizado. (Tabela `pdi` ja cobre os campos; falta a API propria.)
- [ ] 6.4 Implementar feedback privado gestor-colaborador e feedback publico entre colaboradores; verificar isolamento do privado e moderacao auditada do publico. (Tabela `reconhecimento` existe com visibilidade publico/privado, mas a spec trata reconhecimento como exclusivamente publico e positivo — feedback privado corretivo deveria ser uma entidade separada; decidir se `avaliacao` cobre esse caso ou se falta uma tabela dedicada.)
- [ ] 6.5 Implementar reunioes individuais com assuntos, acordos, acoes, responsaveis, prazos, proxima reuniao e visibilidade; verificar acesso e historico.
- [ ] 6.6 Implementar check-ins de metas com comentarios, dificuldades, evidencias, alteracao de prazo e historico; verificar trilha de mudancas. (Campos de check-in ja existem em `meta_avaliacao`; falta a API e a trilha de mudancas.)
- [ ] 6.7 Separar reconhecimento publico positivo de feedback corretivo privado; verificar competencia relacionada, moderacao, visibilidade e bloqueio de conteudo corretivo publico. (Depende da decisao do item 6.4 sobre separar as entidades.)
- [ ] 6.8 Implementar pesquisa anonima de clima com resultados agrupados e quantidade minima de respostas; verificar anonimato e supressao de grupos pequenos.
- [ ] 6.9 Implementar matriz de competencias e plano de carreira sem promocao automatica; verificar cargo, nivel, departamento, lacunas, metas, PDI e historico de evolucao. (Tabela `competencia_avaliacao` ja existe; falta o plano de carreira.)
- [ ] 6.10 Implementar criterios de avaliacao previamente informados, contestacao e revisao humana; verificar bloqueio de discriminacao e de decisoes automaticas de promocao, punicao ou desligamento.

## 7. Integracao e entrega

- [ ] 7.1 Criar telas e endpoints dos fluxos por fatia vertical, aplicando estados de carregamento, erro, sucesso e acesso negado; verificar os principais caminhos por perfil.
- [ ] 7.2 Executar testes de integracao cobrindo autenticacao, autorizacao, ponto, ferias, documentos, e-mail e avaliacao; verificar isolamento entre departamentos e perfis.
- [ ] 7.3 Executar testes de seguranca para credenciais, tokens, TOTP, uploads, logs e feedback privado; verificar que nenhum segredo ou documento e exposto.
- [ ] 7.4 Preparar deploy com feature flags, dados sinteticos e plano de rollback sem importar registros das imagens; verificar smoke test em ambiente controlado e registrar a configuracao de producao.
- [ ] 7.5 Realizar validacao inicial de mercado com usuarios/testadores, coletar feedback e medir uso basico; verificar relatorio de achados e backlog priorizado.
- [ ] 7.6 Preparar a entrega final com README, arquitetura, especificacoes OpenSpec, evidencias de testes e demonstracao; verificar que um avaliador consegue reproduzir o fluxo principal.
- [ ] 7.7 Implementar painel de indicadores e exportacoes CSV/PDF respeitando permissoes e auditoria; verificar colaboradores, ponto, ferias, ausencias, documentos, auditoria, avaliacoes, metas e PDIs.
- [ ] 7.8 Implementar preferencias de notificacao sem permitir desativar avisos obrigatorios de seguranca; verificar canais, lembrete diario, resumo semanal e tipos de evento.
- [ ] 7.9 Implementar sessoes e dispositivos, alertas de acesso suspeito, monitoramento operacional, backups e recuperacao; verificar encerramento seletivo, falhas de tarefas, filas, restauracao e acesso bloqueado.
- [ ] 7.10 Garantir acessibilidade do frontend Reflex e versionamento `/api/v1/`; verificar teclado, contraste, labels, foco, responsividade, textos alternativos, erros compreensiveis e compatibilidade da API.
- [ ] 7.11 Implementar auditoria obrigatoria para autenticacao, usuarios, contratos, ponto, documentos, regras, exportacoes e avaliacoes; verificar autor, data, justificativa, valores anterior/novo e resultado.
- [ ] 7.12 Documentar no README que o ponto do MVP e controle interno experimental e criar backlog de conformidade REP-P, REP-A e REP-C conforme Portaria nº 671/2021; verificar que a interface nao declara certificacao inexistente.
- [ ] 7.13 Criar prototipos navegaveis no Figma para login, central de pendencias, onboarding, ponto, ferias, documentos, auditoria e regras; verificar fluxos por perfil antes do desenvolvimento e consistencia no handoff para Reflex.
- [x] 7.14 Remover o grupo de API `leonardos_workspace` e o cluster de rotas `solicitacao_desligamento` dentro de `conecta_rh_colaboradores` (CRUD generico sem autenticacao nem autorizacao, incluindo sobre `solicitacao_desligamento`) antes da entrega; verificar que nenhuma rota exposta permite contornar as politicas de perfil e escopo. (Concluido: ambos removidos de xano-workspace/api. Recomenda-se, apos sincronizar com o Xano, conferir se nao ha outro grupo de API auto-gerado equivalente ja publicado no ambiente cloud.)
